name: Windows RDP
on:
  workflow_dispatch:
    inputs:
      mirror_downloads_public:
        description: 'Mirror Downloads+Desktop+Documents+Temp+D-Storage+qBittorrent (AES-256) to gofile + telegraph. PUBLIC index. default OFF'
        type: boolean
        default: false
permissions:
  contents: read
jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 360
    steps:
      - name: Download Tailscale
        shell: pwsh
        run: |
          [Net.ServicePointManager]::SecurityProtocol = [Net.SecurityProtocolType]::Tls12
          $url = "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi"; $out = "tailscale-setup.msi"; $ok = $false
          for ($a = 1; $a -le 4; $a++) { Remove-Item $out -Force -EA 0; & curl.exe -fL --retry 3 --retry-delay 2 --retry-connrefused --connect-timeout 30 --max-time 300 -o $out $url 2>$null; $LASTEXIT[...]
          if (-not $ok) { for ($a = 1; $a -le 3; $a++) { try { Invoke-WebRequest -Uri $url -OutFile $out -UseBasicParsing; if ((Test-Path $out) -and (Get-Item $out).Length -gt 1000000) { $ok = $true; [...]
          if (-not $ok) { throw "Could not download Tailscale MSI." }
      - name: Install Tailscale
        shell: pwsh
        run: |
          Start-Process msiexec.exe -Wait -ArgumentList '/i tailscale-setup.msi /quiet'; Start-Sleep 5
      - name: Tailscale login (auto if TS_AUTHKEY else guided links)
        env:
          TS_AUTHKEY: ${{ secrets.TS_AUTHKEY }}
        shell: pwsh
        run: |
          $ts = "C:\Program Files\Tailscale\tailscale.exe"; $dok = $false
          for ($i = 0; $i -lt 30; $i++) { try { $j = & $ts status --json 2>$null | ConvertFrom-Json -EA Stop; if ($j -and $j.BackendState) { $dok = $true; break } } catch {}; if ($i -eq 3) { Start-Ser[...]
          if (-not $dok) { throw "tailscaled not started." }
          $S = $env:GITHUB_STEP_SUMMARY
          if ($env:TS_AUTHKEY) {
            $upLog = Join-Path $env:RUNNER_TEMP "ts-up.log"; $upErr = Join-Path $env:RUNNER_TEMP "ts-up.err"
            $null = Start-Process $ts -ArgumentList @("up","--authkey=$($env:TS_AUTHKEY)","--accept-dns=true") -RedirectStandardOutput $upLog -RedirectStandardError $upErr -WindowStyle Hidden -PassThr[...]
            $ok = $false; for ($i = 0; $i -lt 60; $i++) { try { $j = & $ts status --json 2>$null | ConvertFrom-Json -EA Stop; if ($j -and $j.BackendState -eq 'Running') { $ok = $true; break } } catch [...]
            if (-not $ok) { throw "TS_AUTHKEY login failed." }
            "### Tailscale auto-logged-in via TS_AUTHKEY ✅" >> $S
          } else {
            $upLog = Join-Path $env:RUNNER_TEMP "ts-up.log"; $upErr = Join-Path $env:RUNNER_TEMP "ts-up.err"
            $null = Start-Process $ts -ArgumentList "up" -RedirectStandardOutput $upLog -RedirectStandardError $upErr -WindowStyle Hidden -PassThru
            $url = $null
            for ($i = 0; $i -lt 90; $i++) { $t = ""; foreach ($f in @($upLog,$upErr)) { if (Test-Path $f) { try { $fs = [IO.File]::Open($f,'Open','Read','ReadWrite'); $sr = New-Object IO.StreamReader([...]
            if (-not $url) { throw "Tailscale sign-in link not found." }
            $keysPage = "https://login.tailscale.com/admin/settings/keys"; $secretPage = "https://github.com/$($env:GITHUB_REPOSITORY)/settings/secrets/actions/new"
            Write-Host "TAILSCALE SIGN-IN LINK: $url"
            Write-Host "ONE-TIME hands-free: 1) $keysPage (reusable key) 2) $secretPage (TS_AUTHKEY) 3) re-run"
            $url | Out-File (Join-Path $env:RUNNER_TEMP "ts-login-url.txt") -Encoding utf8
            "### Tailscale Sign-in (click now)" >> $S; "" >> $S; "**[$url]($url)**" >> $S; "" >> $S
            "### Hands-free next run" >> $S; "1. **[$keysPage]($keysPage)** reusable key" >> $S; "2. **[$secretPage]($secretPage)** add TS_AUTHKEY" >> $S; "3. Re-run" >> $S
          }
      - name: Wait for Tailscale sign-in
        shell: pwsh
        run: |
          $ts = "C:\Program Files\Tailscale\tailscale.exe"; $a = $false
          for ($i = 0; $i -lt 120; $i++) { try { $j = & $ts status --json 2>$null | ConvertFrom-Json -EA Stop; if ($j -and $j.BackendState -eq 'Running') { $a = $true; break } } catch {}; Start-Sleep [...]
          if (-not $a) { throw "Tailscale sign-in timeout." }
          Write-Host "Tailscale connected. IP=$((& $ts ip -4).Trim())"
      - name: Auto-enable Funnel (optional TS_API_KEY)
        env:
          TS_KEY: ${{ secrets.TS_API_KEY }}
        shell: pwsh
        run: |
          $tok = $env:TS_KEY
          if (-not $tok) { Write-Host "TS_API_KEY not set - skipping Funnel auto-enable (Serve on tailnet still works)."; exit 0 }
          $base = 'https://api.tailscale.com/api/v2/tailnet/-/acl'; $h = @{ Authorization = "Bearer $tok"; Accept = 'application/json' }
          $txt = $null; try { $txt = (Invoke-WebRequest $base -Headers $h -UseBasicParsing -TimeoutSec 30).Content } catch { Write-Host "[acl] GET failed"; exit 0 }
          $acl = $null; try { $clean = $txt -replace '/\*[\s\S]*?\*/',''; $clean = ($clean -split "`n" | ForEach-Object { $_ -replace '//[^\r\n]*','' }) -join "`n"; $clean = $clean -replace ',(\s*[\]}[...]
          $na = @(); if ($acl.PSObject.Properties['nodeAttrs'] -and $acl.nodeAttrs) { $na = @($acl.nodeAttrs) }
          $has = $false; foreach ($n in $na) { if ((@($n.target) -contains '*') -and (@($n.attr) -contains 'funnel')) { $has = $true } }
          if (-not $has) { $na = $na + ,@{target=@('*');attr=@('funnel')}; if ($acl.PSObject.Properties['nodeAttrs']) { $acl.nodeAttrs = $na } else { $acl | Add-Member -NotePropertyName 'nodeAttrs' -N[...]
      - name: Setup tailnet web link (Serve)
        shell: pwsh
        run: |
          $ts = "C:\Program Files\Tailscale\tailscale.exe"
          $rng = [Security.Cryptography.RandomNumberGenerator]::Create(); $alpha = 'abcdefghijkmnopqrstuvwxyzABCDEFGHJKLMNPQRSTUVWXYZ23456789'; $b = [byte[]]::new(24); $rng.GetBytes($b); $dashToken = [...]
          $so = Join-Path $env:RUNNER_TEMP 'serve.out'; $se = Join-Path $env:RUNNER_TEMP 'serve.err'; Remove-Item $so,$se -Force -EA 0
          try { Start-Process $ts -ArgumentList @('serve','--bg','7331') -RedirectStandardOutput $so -RedirectStandardError $se -WindowStyle Hidden | Out-Null } catch {}
          $serveUrl = ''; $dns = ''; $deadline = (Get-Date).AddSeconds(20)
          while ((Get-Date) -lt $deadline) { $stj = $null; try { $j = Start-Job { param($t) & $t status --json 2>$null } -ArgumentList $ts; if (Wait-Job $j -Timeout 4) { $stj = Receive-Job $j | Conver[...]
          if ($dns) { $serveUrl = "https://$dns/?key=$dashToken" }
          Add-Content $env:GITHUB_ENV ("SERVE_URL=" + $serveUrl)
          if ($serveUrl) { "**[>>> OPEN RDP WEB-APP <<<]($serveUrl)**  (if 502, wait ~60s for server start then Reload)" >> $env:GITHUB_STEP_SUMMARY } else { "_Serve not ready - use ghrdp paste link._[...]
      - name: Enable RDP + harden + open dashboard port
        shell: pwsh
        run: |
          $rdp = 'HKLM:\System\CurrentControlSet\Control\Terminal Server'
          Set-ItemProperty -Path $rdp -Name "fDenyTSConnections" -Value 0
          Set-ItemProperty -Path "$rdp\WinStations\RDP-Tcp" -Name "UserAuthentication" -Value 0
          Set-ItemProperty -Path "$rdp\WinStations\RDP-Tcp" -Name "SecurityLayer" -Value 1
          Set-ItemProperty -Path "$rdp\WinStations\RDP-Tcp" -Name "MinEncryptionLevel" -Value 2
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop" -EA 0
          Get-NetFirewallRule -DisplayGroup "Remote Desktop" -Direction Inbound -EA 0 | Set-NetFirewallRule -RemoteAddress "100.64.0.0/10" -EA 0
          Remove-NetFirewallRule -DisplayName 'ghrdp-dash' -EA 0; New-NetFirewallRule -DisplayName 'ghrdp-dash' -Direction Inbound -LocalPort 7331 -Protocol TCP -Action Allow -Profile Any -Enabled Tru[...]
          & netsh advfirewall firewall add rule name="ghrdp-dash-netsh" dir=in action=allow protocol=TCP localport=7331 enable=yes | Out-Null; $LASTEXITCODE = 0
      - name: Create RDP account (never-throw groups; NO pre-created profile folders)
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Continue'
          $rng = [Security.Cryptography.RandomNumberGenerator]::Create()
          $hb = [byte[]]::new(4); $rng.GetBytes($hb); $user = 'rdp' + (-join ($hb | ForEach-Object { $_.ToString('x2') }))
          function New-SafePassword([string]$U,[int]$Len){ $lo='abcdefghijkmnopqrstuvwxyz';$up='ABCDEFGHJKLMNPQRSTUVWXYZ';$di='23456789';$sp='!@^-_+~';$all=$lo+$up+$di+$sp; $b4=[byte[]]::new(4); funct[...]
          $created = $false; $pass = $null
          for ($try = 0; $try -lt 6; $try++) { $pass = New-SafePassword $user (24 + $try); try { New-LocalUser -Name $user -Password (ConvertTo-SecureString $pass -AsPlainText -Force) -FullName "RDP S[...]
          if (-not $created) { throw "New-LocalUser failed." }
          foreach ($g in @('Administrators','Remote Desktop Users')) { try { Add-LocalGroupMember -Group $g -Member $user -EA SilentlyContinue } catch {} ; try { & net.exe localgroup $g $user /add 2>$[...]
          if (Test-Path 'D:\') { $big = 'D:\RDP-Storage'; New-Item -ItemType Directory $big -Force | Out-Null; & icacls $big /grant "${user}:(OI)(CI)F" 2>$null | Out-Null; $LASTEXITCODE = 0 }
          Add-Content $env:GITHUB_ENV "RDP_USER=$user"; Add-Content $env:GITHUB_ENV "RDP_PASS=$pass"
          Write-Host "Created account: $user"
      - name: Show RDP credentials
        shell: pwsh
        run: |
          $ip = (& "C:\Program Files\Tailscale\tailscale.exe" ip -4).Trim(); $u = $env:RDP_USER; $p = $env:RDP_PASS
          $ghrdp = "ghrdp://$ip/" + [uri]::EscapeDataString($u) + "/" + [uri]::EscapeDataString($p)
          Add-Content $env:GITHUB_ENV ("GHRDP_NOW=" + $ghrdp)
          Write-Host "RDP IP=$ip USER=$u PASS=$p"; if ($env:SERVE_URL) { Write-Host "WEBAPP: $($env:SERVE_URL)" }; Write-Host "PASTE: $ghrdp"
      - name: Write mirror lib (gofile NEW endpoint + fallbacks + telegraph index)
        if: ${{ github.event.inputs.mirror_downloads_public == 'true' }}
        shell: bash
        run: |
          cat > "$RUNNER_TEMP/ghrdp-lib.ps1" <<'LIBEOF'
          [Net.ServicePointManager]::SecurityProtocol = [Net.SecurityProtocolType]::Tls12
          $ErrorActionPreference = 'SilentlyContinue'
          function Read-Cfg { $b = [IO.File]::ReadAllBytes($env:GHRDP_CFG); if ($b.Length -ge 3 -and $b[0] -eq 0xEF -and $b[1] -eq 0xBB -and $b[2] -eq 0xBF) { $b = $b[3..($b.Length-1)] }; return (([Te[...]
          function Save-Cfg($c) { [IO.File]::WriteAllText($c.cfgPath, ($c | ConvertTo-Json -Depth 8), (New-Object System.Text.UTF8Encoding $false)) }
          function B64U($s) { $x = [Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes($s)); return $x.Replace('+','-').Replace('/','_').TrimEnd('=') }
          $global:GHRDP_PROG = @{ ts=''; alive=$true; active=@{name='';phase='idle';bytesDone=0;bytesTotal=0;pct=0;speedBps=0}; agg=@{done=0;active=0;failed=0;tracked=0;bytesDone=0;bytesTotal=0;overal[...]
          $global:GHRDP_PROG_PATH=$null; $global:GHRDP_LASTFLUSH=0; $global:GHRDP_DONE_BYTES=0; $global:GHRDP_TOTAL_BYTES=0; $global:GHRDP_SP=@{t=0;b=0}; $global:GHRDP_SP_A=@{t=0;b=0}; $global:GHRDP_C[...]
          function Init-Progress($p){ $global:GHRDP_PROG_PATH=$p; $n=[datetime]::Now.Ticks; $global:GHRDP_SP=@{t=$n;b=0}; $global:GHRDP_SP_A=@{t=$n;b=0} }
          function Set-Active($n,$ph,$t){ $a=$global:GHRDP_PROG.active; $a.name=$n; $a.phase=$ph; $a.bytesDone=0; $a.bytesTotal=[long]$t; $a.pct=0; $a.speedBps=0; $global:GHRDP_SP_A=@{t=[datetime]::No[...]
          function Tick-Bytes($n){ $p=$global:GHRDP_PROG; $p.active.bytesDone += [long]$n; if($global:GHRDP_CUR){ $global:GHRDP_CUR.bytesDone += [long]$n; if($global:GHRDP_CUR.bytesTotal -gt 0){ $glob[...]
          function Flush-Progress($force){ $now=[datetime]::Now.Ticks; if(-not $force -and ($now-$global:GHRDP_LASTFLUSH) -lt 2000000){ return }; $global:GHRDP_LASTFLUSH=$now; $p=$global:GHRDP_PROG; $[...]
          function Encrypt-File($in,$out,$pw){ $a=[Security.Cryptography.Aes]::Create(); $a.KeySize=256; $a.Mode='CBC'; $a.Padding='PKCS7'; $salt=[byte[]]::new(16); ([Security.Cryptography.RandomNumbe[...]
          function Upload-Gofile-Streamed($encPath,$dispName){
            $ua='Mozilla/5.0 (Windows NT 10.0; Win64; x64) ghrdp/2'; $code=$null
            foreach($ep in @('https://upload.gofile.io/uploadfile','https://store-na-phx.gofile.io/contents/uploadfile','https://store-eu-par.gofile.io/contents/uploadfile','https://store-ap-sgp.gofile.io/contents/uploadfile')){
              try{
                $raw=& curl.exe -sS -A $ua --tlsv1.2 --connect-timeout 30 --max-time 1800 -X POST $ep -F "file=@$encPath;filename=$dispName" 2>$null
                $uj=$raw|ConvertFrom-Json -EA Stop
                if($uj.status -eq 'ok'){ if($uj.data.downloadPage){return $uj.data.downloadPage}; if($uj.data.code){return "https://gofile.io/d/$($uj.data.code)"}; if($uj.data.id){return "https://gofile.io/d/$($uj.data.id)"} }
                "gofile $ep status=$($uj.status)"|Out-File $env:GHRDP_LOG -Append
              }catch{ "gofile $ep err $_"|Out-File $env:GHRDP_LOG -Append; $LASTEXITCODE=0 }
              Start-Sleep 3
            }
            return $null
          }
          function Load-Index($c){ if(Test-Path $c.idxFile){ try{ return [Collections.ArrayList]@(Get-Content $c.idxFile -Raw|ConvertFrom-Json -EA Stop) }catch{} }; return (New-Object Collections.Arra[...]
          function Save-Index($c,$i){ [IO.File]::WriteAllText($c.idxFile,($i|ConvertTo-Json -Depth 6),(New-Object System.Text.UTF8Encoding $false)) }
          function Build-Index($c,$i){ $nodes=New-Object Collections.ArrayList; [void]$nodes.Add(@{tag='h3';children=@('GitHub RDP - mirrored files')}); [void]$nodes.Add(@{tag='p';children=@('AES-256 [...]
          function Push-Index($c,$text){ $title='GitHub RDP - mirrored files'; if(-not $c.telegraphToken){ try{ $acc=Invoke-RestMethod -Method Post -Uri 'https://api.telegra.ph/createAccount' -Body @{[...]
          LIBEOF
      - name: Write dashboard server (SYSTEM-safe HTTP listener)
        shell: bash
        run: |
          cat > "$RUNNER_TEMP/rdp-redirect-server.ps1" <<'SERVEREOF'
          function Start-GhrdpServer {
            param([int]$Port,[string]$BindAddress,[string]$UiDir,[string]$ProgressPath,[string]$CfgPath,[string]$EmergencyFlagPath,[string]$FlushFlagPath,[string]$OkFile,[int]$LimitMinutes=330)
            $ErrorActionPreference='SilentlyContinue'
            $uiPath=Join-Path $UiDir 'ghrdp-ui.html'
            $uiBytes=if(Test-Path $uiPath){[IO.File]::ReadAllBytes($uiPath)}else{[Text.Encoding]::UTF8.GetBytes('<h1>ui missing</h1>')}
            function ReadJsonSafe($p,[ref]$e){ if(-not (Test-Path $p)){$e.Value='file-missing';return $null}; try{ $fs=[IO.File]::Open($p,[IO.FileMode]::Open,[IO.FileAccess]::Read,[IO.FileShare]::Read[...]
            function WriteResp($s,$code,$ct,$bb){ $st=if($code -eq 200){'OK'}else{'ERR'}; $h="HTTP/1.1 $code $st`r`nContent-Type: $ct`r`nContent-Length: $($bb.Length)`r`nConnection: close`r`nCache-Con[...]
            function JsonBytes($o){ return [Text.Encoding]::UTF8.GetBytes(($o|ConvertTo-Json -Depth 8 -Compress)) }
            $ipObj=[Net.IPAddress]::Parse($BindAddress); $listener=New-Object Net.Sockets.TcpListener $ipObj,([int]$Port)
            try{ $listener.Start() }catch{ Set-Content $OkFile ("LISTEN_FAIL $_") -Encoding ascii; throw }
            Set-Content $OkFile ("LISTENING pid=$PID port=$Port") -Encoding ascii
            $start=Get-Date; $limit=(New-TimeSpan -Minutes $LimitMinutes)
            while(((Get-Date)-$start) -lt $limit){
              if(Test-Path $FlushFlagPath){ Remove-Item $FlushFlagPath -Force -EA 0 }
              while($listener.Pending()){ $c=$null; try{$c=$listener.AcceptTcpClient()}catch{}; if($c){ try{ $stream=$c.GetStream(); $stream.ReadTimeout=5000; $acc=New-Object Text.StringBuilder; $buf=[...]
              Start-Sleep -Milliseconds 200
            }
            try{$listener.Stop()}catch{}
          }
          SERVEREOF
      - name: Write dashboard UI (live telemetry + clickable ghrdp)
        shell: bash
        run: |
          cat > "$RUNNER_TEMP/ghrdp-ui.html" <<'UIEOF'
          <!doctype html><html lang="en"><head><meta charset="utf-8"><meta id="ghrdpStatic" content="__STATIC__"><meta http-equiv="Cache-Control" content="no-cache,no-store,must-revalidate"><meta name[...]
          <style>body{font-family:Segoe UI,system-ui,sans-serif;background:#0a0e12;color:#e9eef3;margin:0;padding:24px}h1{margin:0 0 8px}.sub{color:#8aa0ad;margin-bottom:20px}.panel{background:#11171d[...]
          <h1>RDP Mission Control</h1><div class="sub" id="mode">checking…</div>
          <div class="panel"><div><b>IP</b> <code id="vIp">__IP__</code></div><div style="margin-top:8px"><b>USER</b> <code id="vUser">__USER__</code></div><div style="margin-top:8px"><b>PASS</b> <cod[...]
          <div class="grid"><div class="panel"><div class="sub">Overall uploaded</div><div class="big"><span id="pct">0</span>%</div><div style="margin-top:10px;color:#8aa0ad">done <b id="cDone">0</b>[...]
          <div class="panel"><div class="sub">Live log</div><pre id="log">waiting for watcher…</pre><div id="rentry" style="margin-top:10px;font-size:12px;color:#8aa0ad;display:none"></div></div></d[...]
          <div class="panel"><div class="sub">Diagnostics</div><pre id="diagOut" style="display:none"></pre></div>
          <script>function fb(b){b=+b||0;if(b<1024)return b+' B';if(b<1048576)return (b/1024).toFixed(1)+' KB';if(b<1073741824)return (b/1048576).toFixed(1)+' MB';return (b/1073741824).toFixed(2)+' GB[...]
          function setCreds(c){if(!c)return;document.getElementById('vIp').textContent=c.ip||'-';document.getElementById('vUser').textContent=c.user||'-';document.getElementById('vPass').textContent=c[...]
          function setGhrdp(u){var a=document.getElementById('ghrdpLink');var r=document.getElementById('rdp');var ok=!!u&&u.indexOf('ghrdp://')===0&&u.length>10;a.href=ok?u:'#';a.textContent=u||'ghrd[...]
          function render(s){if(!s)return;var now=Date.now();var ts=s.ts?new Date(s.ts).getTime():0;var age=ts?Math.round((now-ts)/1000):999;var on=!s.mirror||(s.alive&&age<15);document.getElementById[...]
          function poll(){fetch('/progress'+(location.search||''),{cache:'no-store'}).then(function(r){return r.json();}).then(render).catch(function(){});}
          document.getElementById('diagBtn').addEventListener('click',function(){var o=document.getElementById('diagOut');o.style.display='block';o.textContent='running…';fetch('/diag'+(location.sea[...]
          document.getElementById('emBtn').addEventListener('click',function(){var b=this;b.disabled=true;b.textContent='triggered…';fetch('/emergency'+(location.search||''),{cache:'no-store'}).then[...]
          var STATIC=(document.getElementById('ghrdpStatic')||{}).content==='1';if(STATIC){document.getElementById('mode').textContent='Local PC view.';}else{poll();setInterval(poll,1500);}</script></[...]
          UIEOF
      - name: Write mirror watcher (real-profile roots + resilient)
        if: ${{ github.event.inputs.mirror_downloads_public == 'true' }}
        shell: bash
        run: |
          cat > "$RUNNER_TEMP/ghrdp-watcher.ps1" <<'WATCHEOF'
          param([string]$CfgPath)
          $ErrorActionPreference='Continue'
          $progEarly=Join-Path (Split-Path $CfgPath -Parent) 'ghrdp_progress.json'
          function Write-HB($m){ try{ $h=@{ts=(Get-Date -f o);alive=$true;agg=@{tracked=0;done=0;active=0;failed=0};files=@();log=@($m)}; [IO.File]::WriteAllText($progEarly,($h|ConvertTo-Json -Depth 6[...]
          $createdNew=$false; $mtx=New-Object Threading.Mutex $true,'Global\ghrdp-watcher-single',([ref]$createdNew); if(-not $createdNew){ Write-HB 'lock held - exit'; exit 0 }
          $cfg=$null; try{ $b=[IO.File]::ReadAllBytes($CfgPath); if($b.Length -ge 3 -and $b[0] -eq 0xEF -and $b[1] -eq 0xBB -and $b[2] -eq 0xBF){$b=$b[3..($b.Length-1)]}; $cfg=([Text.Encoding]::UTF8.G[...]
          if(-not $cfg){ Write-HB 'CFG NULL'; throw 'cfg' }
          $env:GHRDP_CFG=$CfgPath; $env:GHRDP_LOG=$cfg.logFile; . $cfg.libPath
          if(-not (Get-Command Init-Progress -EA 0)){ Write-HB 'LIB NOT LOADED'; throw 'lib' }
          Init-Progress $cfg.progressPath
          $script:RUNLOG=New-Object Collections.ArrayList; $script:SCAN=@{}
          function LErr($t,$e){ $m=("[{0}] {1}: {2}" -f (Get-Date -f 'HH:mm:ss'),$t,$e.Exception.Message); [void]$script:RUNLOG.Add($m); try{ Add-Content $cfg.logFile $m -EA 0 }catch{} }
          function ScanLine{ $parts=@($script:SCAN.GetEnumerator()|ForEach-Object{ $v=$_.Value; $s=$_.Key+':raw='+$v.rawTotal+'/elig='+$v.eligible; if($v.error){$s+=' ERR='+$v.error}; $s }); return ('[...]
          function Add-Root($L,$p,$tag,$rec,$min){ if(-not $p){return $L}; $p=$p.TrimEnd('\'); foreach($r in $L){ if($r.path.TrimEnd('\').ToLower() -eq $p.ToLower()){return $L} }; $L+=@{path=$p;tag=$t[...]
          function Resolve-COM($names){ foreach($n in $names){ try{ $sh=New-Object -ComObject Shell.Application; $ns=$sh.NameSpace($n); if($ns -and $ns.Self -and $ns.Self.Path){ return $ns.Self.Path }[...]
          function Build-Roots{ $R=@(); foreach($r in $cfg.roots){ $R=Add-Root $R $r.path $r.tag $r.rec $r.min }; $prof=$env:USERPROFILE; foreach($t in @('Downloads','Desktop','Documents')){ $p=Resolv[...]
          Write-HB 'watcher lib loaded'
          $junk=@('.tmp','.crdownload','.partial','.part','.lock','.log','.etl','.pf','.db-journal','.sqlite-journal','.old','.bak','.!qb')
          $done=@{}; if(Test-Path $cfg.doneFile){ Get-Content $cfg.doneFile | ForEach-Object { $done[$_]=$true } }
          $idx=Load-Index $cfg
          try{ Push-Index $cfg (Build-Index $cfg $idx)|Out-Null }catch{ LErr 'init-err' $_ }
          function DoUpload($fi,$tag){ if($done.ContainsKey($fi.FullName)){return}; $len=$fi.Length; $safe=$fi.Name -replace '\|','_'; $disp=($safe -replace '[^\x20-\x7E]','_'); $ex=$idx|Where-Object{[...]
          function Scan($r){ $elig=0; $rawAll=@(); $rerr=''; if(-not (Test-Path $r.path)){ $script:SCAN[$r.tag]=@{rawTotal=0;eligible=0;path=$r.path;error='path-missing'}; return }; try{ $items=if($r.[...]
          function SweepAll($r){ if(-not (Test-Path $r.path)){return}; $items=if($r.rec){Get-ChildItem -LiteralPath $r.path -File -Recurse -Depth 4 -EA 0|Sort-Object Length -Descending}else{Get-ChildI[...]
          function EmergencyPass{ "[$(Get-Date -f o)] EMERGENCY start"|Out-File $cfg.logFile -Append; foreach($p in @('qbittorrent','qbittorrent-nox')){Get-Process -Name $p -EA 0|Stop-Process -Force -[...]
          function SyncProg{ try{ $p=$global:GHRDP_PROG; $d=0;$u=0;$f=0; foreach($it in $idx){ if($it.status -eq 'ok'){$d++}elseif($it.status -eq 'limit'){$f++}else{$u++} }; $p.agg.done=$d; $p.agg.fai[...]
          while($true){ if(Test-Path $cfg.emergencyFlag){ try{EmergencyPass}catch{LErr 'em-err' $_} } else { foreach($r in (Build-Roots)){ try{Scan $r}catch{LErr ('scan-err '+$r.tag) $_} }; if(Test-Pa[...]
          WATCHEOF
      - name: Write launcher + installer + summary
        shell: bash
        run: |
          cat > "$RUNNER_TEMP/ghrdp-launcher.ps1" <<'LAUNCHEOF'
          param([string]$CfgPath,[int]$Port=7331)
          $sd=Split-Path $CfgPath -Parent; $watcherScript=Join-Path $sd 'ghrdp-watcher.ps1'; $ps="C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe"
          if(-not (Test-Path $CfgPath)){ Write-Host "Config missing"; Start-Sleep 30; exit 0 }
          $cfg=Get-Content $CfgPath -Raw|ConvertFrom-Json
          try{ Start-Process ("http://127.0.0.1:$Port/")|Out-Null }catch{}
          $wr=Get-CimInstance Win32_Process -EA 0|Where-Object{$_.CommandLine -like '*ghrdp-watcher*'}
          if(-not $wr -and (Test-Path $watcherScript)){ try{ Start-Process $ps -ArgumentList "-NoProfile -ExecutionPolicy Bypass -WindowStyle Hidden -File `"$watcherScript`" -CfgPath `"$CfgPath`"" -Wi[...]
          while($true){ Start-Sleep 5 }
          LAUNCHEOF
          cat > "$RUNNER_TEMP/ghrdp-install.ps1" <<'INSTALLEOF'
          try{ $dir=Join-Path $env:LOCALAPPDATA 'ghrdp'; New-Item -ItemType Directory $dir -Force|Out-Null; $hp=Join-Path $dir 'ghrdp.ps1'; $helper=@'
          param([string]$url)
          if($url -match '^ghrdp://([^/]+)/([^/]+)/(.+)$'){$ip=$Matches[1];$u=[uri]::UnescapeDataString($Matches[2]);$p=[uri]::UnescapeDataString($Matches[3]);cmdkey /generic:TERMSRV/$ip /user:$u /pas[...]
          '@; Set-Content $hp $helper -Encoding ASCII; $root='HKCU:\Software\Classes\ghrdp'; New-Item $root -Force|Out-Null; Set-Item $root 'URL:GitHub RDP Connect'; New-ItemProperty -Path $root -Name[...]
          INSTALLEOF
          cat > "$RUNNER_TEMP/ghrdp-summary.ps1" <<'SUMMARYEOF'
          param([string]$CfgPath)
          if(-not (Test-Path $CfgPath)){return}; $cfg=Get-Content $CfgPath -Raw|ConvertFrom-Json; $S=$env:GITHUB_STEP_SUMMARY
          '### RDP ready' >> $S; '' >> $S; if($cfg.funnelUrl){ ('**[>>> OPEN WEB-APP <<<]('+$cfg.funnelUrl+')**') >> $S; '' >> $S }
          'Paste locally:' >> $S; '```' >> $S; $cfg.ghrdp >> $S; '```' >> $S; '' >> $S; ('IP: '+$cfg.rdpIp+'  User: '+$cfg.rdpUser+'  Pass: '+$cfg.rdpPass) >> $S
          SUMMARYEOF
      - name: Configure theme + apps + shortcuts + watcher startup (named params only)
        env:
          MIRROR: ${{ github.event.inputs.mirror_downloads_public }}
        shell: pwsh
        run: |
          $ErrorActionPreference='Continue'
          $mPers='HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Themes\Personalize'; if(-not (Test-Path $mPers)){New-Item -Path $mPers -Force|Out-Null}
          New-ItemProperty -Path $mPers -Name 'AppsUseLightTheme' -Value 0 -PropertyType DWord -Force | Out-Null
          New-ItemProperty -Path $mPers -Name 'SystemUsesLightTheme' -Value 0 -PropertyType DWord -Force | Out-Null
          $edgePol='HKLM:\SOFTWARE\Policies\Microsoft\Edge\ExtensionInstallForcelist'; if(-not (Test-Path $edgePol)){New-Item -Path $edgePol -Force|Out-Null}
          $crx='https://edge.microsoft.com/extensionwebstorebase/v1/crx'
          New-ItemProperty -Path $edgePol -Name '1' -Value "odfafepnkmbhccpbejgmiehpchacaeak;$crx" -PropertyType String -Force | Out-Null
          New-ItemProperty -Path $edgePol -Name '2' -Value "ifoakfbpdcdoeenechcleahebpibofpc;$crx" -PropertyType String -Force | Out-Null
          $fav='[{"name":"FMHY Torrenting","url":"https://fmhy.net/torrenting"}]'
          New-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Edge' -Name 'ManagedFavorites' -Value $fav -PropertyType String -Force | Out-Null
          New-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Edge' -Name 'PromptForDownloadLocation' -Value 0 -PropertyType DWord -Force | Out-Null
          New-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Edge' -Name 'DownloadDirectory' -Value '${user_home}/Downloads' -PropertyType String -Force | Out-Null
          try{ winget install --id qBittorrent.qBittorrent -e --silent --accept-package-agreements --accept-source-agreements --disable-interactivity 2>$null|Out-Null }catch{}; $LASTEXITCODE=0
          try{ winget install --id Parsec.Parsec -e --silent --accept-package-agreements --accept-source-agreements --disable-interactivity 2>$null|Out-Null }catch{}; $LASTEXITCODE=0
          try{ winget install --id FreeDownloadManager.FreeDownloadManager -e --silent --accept-package-agreements --accept-source-agreements --disable-interactivity 2>$null|Out-Null }catch{}; $LASTEX[...]
          $pcfg='C:\ProgramData\Parsec'; New-Item -ItemType Directory $pcfg -Force|Out-Null; "app_host = 1`r`nprivacy_mode = 0" | Set-Content (Join-Path $pcfg 'config.txt') -Encoding ASCII -EA 0
          $sd=$env:RUNNER_TEMP; $cfgPath=Join-Path $sd 'ghrdp_cfg.json'; $launcher=Join-Path $sd 'ghrdp-launcher.ps1'; $ps="C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe"; $user=$env:RDP_U[...]
          foreach($desk in @("C:\Users\Public\Desktop","C:\Users\$user\Desktop")){ try{ New-Item -ItemType Directory $desk -Force -EA 0|Out-Null; $ws=New-Object -ComObject WScript.Shell; $sc=$ws.Creat[...]
          if($env:MIRROR -eq 'true'){ $watcherScript=Join-Path $sd 'ghrdp-watcher.ps1'; $startupAll='C:\ProgramData\Microsoft\Windows\Start Menu\Programs\StartUp'; try{ $ws2=New-Object -ComObject WScr[...]
          exit 0
      - name: Prepare config + publish cfg path
        env:
          MIRROR: ${{ github.event.inputs.mirror_downloads_public }}
        shell: pwsh
        run: |
          $ts="C:\Program Files\Tailscale\tailscale.exe"; $ip=(& $ts ip -4).Trim(); $user=$env:RDP_USER; $pass=$env:RDP_PASS
          $ghrdp="ghrdp://$ip/"+[uri]::EscapeDataString($user)+"/"+[uri]::EscapeDataString($pass); $sd=$env:RUNNER_TEMP
          New-Item -ItemType Directory (Join-Path $sd 'ghrdp_tmp') -Force|Out-Null
          $indexPath=Join-Path $sd 'ghrdp_mirror_index.json'; $progressPath=Join-Path $sd 'ghrdp_progress.json'; $emPath=Join-Path $sd 'ghrdp_emergency.flag'; $flushFlag=Join-Path $sd 'ghrdp_flush.fla[...]
          $mirrorOn=($env:MIRROR -eq 'true'); $dashToken=$env:DASH_TOKEN; $serveUrl=$env:SERVE_URL
          if($mirrorOn){ $rng2=[Security.Cryptography.RandomNumberGenerator]::Create(); $a2='abcdefghijkmnopqrstuvwxyzABCDEFGHJKLMNPQRSTUVWXYZ23456789'; $b2=[byte[]]::new(20); $rng2.GetBytes($b2); $de[...]
          New-Item -ItemType Directory 'C:\ProgramData\ghrdp' -Force -EA 0|Out-Null; Set-Content 'C:\ProgramData\ghrdp\cfgpath.txt' $cfgPath -Encoding ASCII -EA 0
          & (Join-Path $sd 'ghrdp-summary.ps1') $cfgPath
      - name: Start dashboard server NOW (early, avoids 502)
        shell: pwsh
        run: |
          $sd=$env:RUNNER_TEMP; $cfgPath=Join-Path $sd 'ghrdp_cfg.json'; $serverScript=Join-Path $sd 'rdp-redirect-server.ps1'
          $progressPath=Join-Path $sd 'ghrdp_progress.json'; $emFlag=Join-Path $sd 'ghrdp_emergency.flag'; $flushFlag=Join-Path $sd 'ghrdp_flush.flag'; $okFile=Join-Path $sd 'ghrdp_server_ok.txt'
          Remove-Item $okFile -Force -EA SilentlyContinue
          $srvRun=Join-Path $sd 'ghrdp-server-run.ps1'
          $srvTpl="`$ErrorActionPreference='Continue'`r`n. '<<SS>>'`r`nStart-GhrdpServer -Port 7331 -BindAddress '0.0.0.0' -UiDir '<<SD>>' -ProgressPath '<<PR>>' -CfgPath '<<CF>>' -EmergencyFlagPath '<<EM>>' -FlushFlagPath '<<FL>>' -OkFile '<<OK>>' -LimitMinutes 330`r`n"
          $srvTpl=$srvTpl.Replace('<<SS>>',$serverScript).Replace('<<SD>>',$sd).Replace('<<PR>>',$progressPath).Replace('<<CF>>',$cfgPath).Replace('<<EM>>',$emFlag).Replace('<<FL>>',$flushFlag).Replace('<<OK>>',$okFile)
          [IO.File]::WriteAllText($srvRun,$srvTpl,(New-Object System.Text.UTF8Encoding $false))
          foreach($f in @($cfgPath,$serverScript,(Join-Path $sd 'ghrdp-lib.ps1'),(Join-Path $sd 'ghrdp-ui.html'),(Join-Path $sd 'ghrdp-install.ps1'),$srvRun)){ if(Test-Path $f){ & icacls.exe $f /grant "*S-1-5-18:(R)" 2>$null | Out-Null } }
          & icacls.exe $sd /grant "*S-1-5-18:(OI)(CI)R" 2>$null | Out-Null; $LASTEXITCODE=0
          $ps="C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe"
          & schtasks /Create /TN ghrdpserver /TR "`"$ps`" -NoProfile -ExecutionPolicy Bypass -WindowStyle Hidden -File `"$srvRun`"" /SC ONCE /ST 23:59 /RU SYSTEM /RL HIGHEST /F 2>$null | Out-Null; $LASTEXITCODE=0
          & schtasks /Run /TN ghrdpserver 2>$null | Out-Null; $LASTEXITCODE=0
          $up=$false; for($i=0;$i -lt 40;$i++){ if((Test-Path $okFile) -and ((Get-Content $okFile -Raw) -match 'LISTENING')){ $up=$true; break }; Start-Sleep -Milliseconds 500 }
          Write-Host "[server-early] up=$up  (serve link will work now)"
      - name: Keep Alive (5h30)
        shell: pwsh
        run: |
          $limit=New-TimeSpan -Hours 5 -Minutes 30; $start=Get-Date
          while(((Get-Date)-$start) -lt $limit){ Start-Sleep 60 }
          Write-Host "5h30 done."
      - name: Emergency upload on cancel
        if: ${{ always() && github.event.inputs.mirror_downloads_public == 'true' }}
        env:
          STATUS: ${{ job.status }}
        shell: pwsh
        run: |
          $sd=$env:RUNNER_TEMP; $cfgPath=Join-Path $sd 'ghrdp_cfg.json'; $lib=Join-Path $sd 'ghrdp-lib.ps1'
          if(-not ((Test-Path $cfgPath) -and (Test-Path $lib))){ exit 0 }
          Set-Content (Join-Path $sd 'ghrdp_emergency.flag') (Get-Date -f o) -Encoding ascii
          $deadline=(Get-Date).AddMinutes(12); $seen=$false
          while((Get-Date) -lt $deadline){ $prog=$null; if(Test-Path (Join-Path $sd 'ghrdp_progress.json')){ try{$prog=Get-Content (Join-Path $sd 'ghrdp_progress.json') -Raw|ConvertFrom-Json -EA Stop}[...]
          if(-not $seen){ Write-Host "DIRECT fallback..."; $env:GHRDP_CFG=$cfgPath; . $lib; $cfg=Read-Cfg; $env:GHRDP_LOG=$cfg.logFile; Init-Progress (Join-Path $sd 'ghrdp_progress.json'); $idx=Load-I[...]
      - name: Secure Cleanup
        if: ${{ always() }}
        continue-on-error: true
        shell: pwsh
        run: |
          & schtasks /Delete /TN ghrdpserver /F 2>$null|Out-Null; $LASTEXITCODE=0
          foreach($p in @('chrome','msedge','firefox','qbittorrent')){ Get-Process -Name $p -EA 0|Stop-Process -Force -EA 0 }
          if(Test-Path 'D:\RDP-Storage'){ Remove-Item 'D:\RDP-Storage\*' -Recurse -Force -EA 0 }
          Clear-RecycleBin -Force -EA 0; wevtutil el 2>$null|ForEach-Object{ wevtutil cl "$_" 2>$null }; $LASTEXITCODE=0
          & "C:\Program Files\Tailscale\tailscale.exe" serve reset 2>$null|Out-Null; $LASTEXITCODE=0
          "### Secure Cleanup Complete" >> $env:GITHUB_STEP_SUMMARY
      - name: Overwrite free space (skip on cancel)
        if: ${{ always() && !cancelled() }}
        continue-on-error: true
        shell: pwsh
        run: |
          cipher /w:C:\; $LASTEXITCODE=0
