# GHRDP STATE (<=60 lines; phase: PRODUCTION MAINTENANCE; masked-secrets first-4)

## Decisions (permanent, locked architecture)
- NLA=1 only; no fPromptForPassword=0, no AuthLvlOverride, no client 'authentication level:i:0|3', no 'prompt for credentials:i:0'.
- F27/F28 exception: dash-authorized single-use 60s ticket + direct tailnet source may return credentials to CredWrite; the SAME sanctioned store path backs the F28 ghrdp://recred one-click recovery verb (fresh ticket -> CredWrite OVERWRITE -> mstsc, no typing/clipboard); see docs/MIGRATION.md sec F27+F28. All other transit/store locks remain (no password URLs/logs/artifacts).
- No MOTW/Zone.Identifier strip; no SmartScreen/ClickFix/uBlock/publisher-bypass evasion; no LocalDevices pre-trust arming.
- No C2 persistence (agent/enroll/launch/accept/diag endpoints stay 404); no public mirror/index of third-party content; no anti-forensics.
- Native mstsc target on Tailscale MagicDNS FQDN with Tailscale LE cert; benign UX (interactive cmdkey once, dashboard button, POST-token handler).

## Done ledger (sha-only; older shas prune to git log)
- F28 CLOSE THE LOGON-RESULT LOOP + ONE-CLICK RECOVERY + FALLBACK GAP (session branch arena/01a0de91-supreme-lamp): GROUND TRUTH - the F27 handoff completes (ticket-redeemed -> credwrite-ok -> rdp-written -> mstsc-started) but the dashboard's LAST RDP LOGON row never received a logon RESULT (the F24 keep-alive collector only carried counts and the 4625 leg never stamped), so a rejected password and a dead collector rendered identically; and a cmdkey prompt that closed storing nothing still fell through to a silent mstsc with a known-absent credential. S1 payloads/ghrdp-server.ps1 owns an INDEPENDENT 30s logon scanner ([F28 §1 scanner-begin/-end], extracted VERBATIM by the F28 gate + the Windows lane + lab cell V): it runs in the server process FROM SERVER START (startup scan before the listener accepts, then every 30s inside the accept loop - never in the keep-alive step; the gate fails if Update-RdpLogonAuthLast ever appears in main.yml), reads 4624 LogonType 10 + 4625 (id/LogonType/Status/SubStatus/FailureReason/TargetUserName ONLY, window anchored at server start-120s, capped 3600s), and stamps result=success (a 4624 newer than the newest 4625) | failed (newest 4625 + its sub-status + subMeaning) | none, ALWAYS with scanTs (stamped before any verdict branch, including the security-log-unreadable probe path) into rdp-logon.json. /api/native-status serves rdpListener.authLast (verbatim) + rdpListener.logonCollector {intervalSec 30, staleSec 90, scans, alive, startedAt, uptimeSec, lastScanAgeSec, probeError} + a request-time LIVE CredSSP probe (credsspLive/credsspLiveWhy/credsspLiveTs from NLA + bound-cert trust + AllowEncryptionOracle + SecurityLayer + MinEncryptionLevel) by merging into $rlOut - the config object is never written back from that route, so there is no writer race with the workflow. S2 payloads/ui.html LAST RDP LOGON row renders result+sub+eventTs+scanTs verbatim ('failed sub=0xC000006A (wrong-password) at <evt> - scanned <ts>'); no authLast while the server is up >90s, or an authLast without scanTs, renders RED 'logon collector not running' - 'scanning...' only inside the 90s startup window; [F28 §2] recoveryDecision() (pure) shows the RECOVERY card ONLY when authLast.result=failed AND sub=0xC000006A AND that failure lands inside 120s of the LAST mstsc-started beacon in the handler chain (a stale pre-run 4625 can never open it); the card's single [FIX &amp; RECONNECT] button POSTs /api/rdp-token (fresh single-use 60s ticket, bearer-gated) and fires ghrdp://recred?server=<fqdn>&user=<u>&t=<ticket> - t is the ONLY secret the recovery URL may carry (stricter than the rdp builder: no &ip=), and the manual valueless-/pass cmdkey line is a copy-only, user-run escape hatch (the gate proves every control in the card is copyById except that one JS-attached button); [F28 §4] credsspVerdict() is now the single source for the CREDSSP row AND the RDP LISTENER credssp checkbox - live probe first (OK (live probe) / WARN (live probe: <why>)), config stamp secondary '(stamp: <cs|none>)', the legacy F23/F24 renders kept verbatim for pre-F28 configs, and 'not reported yet (F21 step: ...)' reachable ONLY when neither a live probe nor a stamp exists (driven by the node matrix). S3 payloads/ghrdp-rdp-launcher.cs Ver 2.5.0.0 (F28 recred+fallback-gap): new verb ghrdp://recred (same target validation + DNS guard; beacon chain recred-redeemed -> credwrite-ok -> rdp-written -> mstsc-started) and the CLOSED FALLBACK GAP - CmdkeyStep now returns 'stored | missing | abort' (a timeout still aborts without a launch and keeps its MessageBox), stored=false retries the one-time ticket redemption ONCE and then CredentialFallbackDecision(storeMissing, ticketPresent, retryOk) - the ONE pure decision function shared by the rdp path, the recred path and the new LAB-ONLY --fallback-selftest harness - selects the native-prompt path: the fallback-mstsc-native-prompt beacon is emitted IMMEDIATELY BEFORE mstsc starts with /prompt, so no path can launch mstsc against a known-absent credential (the old 'OK opens mstsc anyway' dialog text is deleted); the launcher still has NO delete verb and no value-bearing /pass:. S4 ghrdp-server.ps1 beacon allowlist accepts recred-redeemed + fallback-mstsc-native-prompt. S5 launch-gates gains the F28 bash gate step (scanner markers + 30s tick + scanTs-before-verdict ordering + server-side-only assertion + row/correlator/recovery-URL/copy-only/card tokens + recred/decision/beacon-ordering/native-prompt launcher tokens + re-refused /pass:/delete/automation locks + the §4 live-probe tokens) and TWO windows-native steps: 'F28 server-side logon scanner (extract + synthetic events)' (dot-sources the extracted block, drives 0xC000006A => failed/wrong-password, newer type-10 => success, empty + probeError => none with scanTs, type-3 console and pre-window 4625 ignored, no identity field in the verdict, scanTs Z-suffixed) and 'F28 launcher fallback-gap decision matrix (csc compile + --fallback-selftest)' (in-box csc, 4/4 matrix, mstscLaunched=0 cmdkeyCalled=0). Superseded pins (updated, not deleted): F17/F23 gate tokens that pinned the old rdpListener expression + the old csOk/credsspOk one-liners (now the shared credsspVerdict), F17/F18 gate+test CmdkeyStep call shape (int rc -> store outcome), F20 assembly-version/stamp pins (now derive the version from the repo constant and expect the F28 stamp), F27 status-row test (the LAST RDP LOGON row now renders authLast, not authEvents counts) - each rewritten to its F28 contract with the reason in place. S6 LAB PROOF: autologin-lab cell V ([F28 §6]) extracts the scanner verbatim, drives it with synthetic events, compiles the shipped launcher with the in-box csc and runs --fallback-selftest, executes tests/f28-logon-recovery.test.js through the real renderer (10 tests: verbatim row/collector-dead/recovery correlator windows/URL parameter allowlist/decision table/CREDSSP live-probe/stamp) and asserts the server-side-only tick; the lab matrix row (v) + the ::notice:: cells line carry it. S7 VERIFY (PENDING-USER, physical PC): click WINDOWS AUTO-LOGIN once - within 60s the LAST RDP LOGON row must read success (<ts>) with a fresh scanTs, the session fullscreen and RDP USAGE ticking; a failed sub=0xC000006A inside the window raises RECOVERY -> click FIX & RECONNECT -> success row within 60s (beacon chain names each hop). Any other halt is mapped from the beacon chain + authLast + logonCollector; one targeted fix per loop, max 3 loops, stop with log-mapped evidence. Refusals unchanged (no typing/UI automation, no password in URLs/logs/artifacts, no NLA/CredSSP/cert weakening, no credential deletion, no public exposure); the final LIVE line still waits for the user's fullscreen confirmation.
- F24 AUTH-REJECT DISCRIMINATOR (session branch arena/01a0ddfc-supreme-lamp; commits 7bc5740+472c640+99ab9a9+091ce00+7a7c7e9+09b8869+0445e1a+c4fd77d+7e2fb3c): GROUND TRUTH - every server probe is green, client DNS is ok and the launcher chain completes (cmdkey stored -> .rdp written -> mstsc started) yet mstsc still returns 0x904/0x7 with the client log reading TcpStateFrontAuth -> TcpStateFailure, i.e. the server REJECTS at the NLA/CredSSP phase; because RDP_PASS + username + FQDN are generated per run, a STALE stored TERMSRV password for the current FQDN produces exactly this signature, so the phase had to discriminate server-side credential bug vs stale client-stored credential vs TLS layer instead of guessing (the old CREDSSP row rendered the ambiguous 'FAIL: pending'). S1 main.yml new step 'Server-side credential proof (F24 - ValidateCredentials against local SAM)' placed AFTER the F21 CredSSP step (the F17 probe rebuilds rdpListener wholesale, so the proof is stamped after the last writer) - Test-LocalSamCredential uses System.DirectoryServices.AccountManagement PrincipalContext('Machine').ValidateCredentials($UserName,$Password) and writes config rdpListener.credValid=true|false + credValidAt/credValidWhy/credValidProbeError (never the password); a probe error (missing-user-or-password-env|sdam-unavailable) and credValid=false both ::error:: + throw ('server-side credential invalid - user creation bug') AFTER the write, so a dead credential can never be advertised. S2 main.yml keep-alive 60s collector ([F24 §2 collector-begin/-end]) reads the runner's OWN Security 4624/4625 into rdpListener.authEvents {ts,windowSec,count4624,count4625,codes[{code,count,meaning}],last4625At,verdict,probeError}: 0xC000006A/0xC000006D => credential-mismatch, 0xC0000064 => no-such-user, 0xC000015B => logon-type-denied, else unclassified; unreadable log => probeError + verdict none; it prints counts/status codes ONLY (no user name, password, domain or address) and writes a step summary only when the verdict changes. S3 payloads/ui.html new AUTH REJECT DISCRIMINATOR row id=rdpAuthRow (paintAuthDiscriminator from {credValid,credsspStatus,authEvents}): red 'SERVER credential bug - re-dispatch' (copy-lines hidden), yellow 'YOUR stored password is stale' with the 'copy CURRENT password from KEYS' control and the cmdkey /delete:TERMSRV/<fqdn> copy-line wired to the live FQDN (the ZERO tool-side deletion path - the user runs it), yellow 'TLS-layer suspect (no 4625 logged on the runner)' with the wevtutil CAPI2/System export copy-lines, an honest 'not reported yet' pending state pointing at the F24/F21 step, and credsspStatus shown verbatim (ok | ok-with-cipher-warn | warn + why) - the bare "FAIL: pending" rendering is DELETED. S4 the launcher is untouched: NO delete verb (locked), Ver 2.3.0.0, cmdkey-stored log proof kept. S5 launch-gates F24 gate pins the S1 probe + write-before-loud-fail + no password printing, the S2 tokens + a secret-free scan (no RDP_PASS/RDP_USER/TargetUserName/SubjectUserName/Password/IpAddress in the collector), the S3 three branches + copy-only row (every control is copyById) + no exec tokens + no "'FAIL: '+cs", and no cmdkey delete in the launcher; every prior gate kept. S6 LAB PROOF (autologin-lab.yml cell U; run 36247463932 ALL steps success, matrix a..t + u=pass; launch-gates 36247464055 green incl. the new windows-native 'Parse the F24 extracted blocks' syntax step): the cell extracts the S1/S2 blocks VERBATIM, creates a real local SAM user and asserts valid=true for the right password / false (no probe error) for a wrong one / the missing-env probe verdict, drives the collector with SYNTHETIC 4625/4624 event XML (6A/6D=>credential-mismatch, 15B=>logon-type-denied, 4624-only=>none) and proves the summary can never carry the decoy user name, then executes tests/f24-auth-discriminator.test.js (11 tests: the SHIPPED renderer run under a DOM stub renders all three branches). Three bring-up lab failures were fixed in place, not papered over: the fixture password contained a 3-char substring of the account name (local policy rejects it - now mirrored from New-RdpPassword), New-LocalUser -Description exceeded 48 chars, and the secret-free scan matched the reason SLUG 'wrong-password' instead of a JSON key. S7 VERIFY (PENDING-USER, physical PC): copy the CURRENT password from KEYS -> cmdkey /delete:TERMSRV/<fqdn> -> WINDOWS AUTO-LOGIN -> paste the copied password exactly -> Connect; green (fullscreen + RDP USAGE ticks) => only then the final line; still 0x904 => read the matrix branch (4625 present? credValid? CAPI2/System exports) and land exactly ONE targeted fix (user-creation bug | client trust store | TLS policy), max 3 loops, stop with log-mapped evidence; cascade-guard lines are never the cause. CONSOLIDATION: the parallel F24 session (arena/01a0de0f, its own §1 step + §2 collector + §3 authDecision core + F24 gate + lab cell U + tests/f24-auth-reject.test.js) independently went ALL GREEN (9/9 node matrix incl. legacy-string statuses + fqdn fallback, 22/22 gate bash blocks, 7/7 gate mutations caught, lab matrix 16/16 u=pass, ValidateCredentials fixture green on a real runner) BEFORE merging; the two implementations were then diffed and this landed version is the superset (rendered-row proofs under a DOM stub, the probe-fault branch, write-before-loud-fail pin, and a live-FQDN-wired cmdkey line), so the parallel branch's overlapping content was CONSOLIDATED (merge 19f2a32; its test file retired in favor of this f24-auth-discriminator.test.js; zero net behavioral delta; no re-open needed). Refusals unchanged: no credential-UI automation, no NLA/CredSSP weakening, no certificate-validation bypass, no credentials in URLs/logs/artifacts, no direct main push from cloud.
- F22 MAIN-LANDING PROOF + PROOF-LANE DISPATCH + CREDSSP VERIFY LOOP (session branch arena/01a0ddc1-supreme-lamp): §0 GROUND TRUTH re-verified from the public API + a full (unshallowed) local clone - F21 is ON MAIN as merge 9dfc71bb862c87a4efc9666b4d23d4d2ac64e4dc (PR #44 "F21: CredSSP/NLA handshake diagnosis + auto-fix", merged 2026-09-26T12:12:19Z, touching launch-gates.yml/main.yml/docs/MIGRATION.md/payloads/ui.html/tests/f12-closeout.test.js/tests/f17-listener.test.js; `git merge-base --is-ancestor 9dfc71b origin/main` = YES), and the gates lane on that merge is GREEN (launch-gates run 36241188768, main@push, job "Native UI and VPS contracts (Node)" + all 30 gate steps incl. "F21 CredSSP/NLA handshake verification gates" success; autologin-lab run 36241188777 on the same push: all 21 steps success). §1 LANDING RULES restated and obeyed - cloud path is commit -> push session branch -> PR -> merge_method=merge (that is how main updates); direct `git push origin main` from cloud is 403 by design and was NOT attempted; no force-push/amend of pushed commits/remote edits anywhere in this session. §2 PROOF-LANE DISPATCH = USER CLICK, token-proven: `gh workflow run main.yml --ref main` returned HTTP 403 "Resource not accessible by integration" on actions/workflows/347767276/dispatches (intent once, retry once, exit recorded; the same 403 comes back for autologin-lab) => this session token has no actions:write, so the F21 lab proof needs the user to click GitHub -> Actions -> "Windows RDP - Tailscale Mission Control (6h ephemeral)" -> Run workflow (ref main). The one in-flight dispatch (run 36236759283) is PRE-F21 (headSha d3243ac = the PR #43 merge) and is explicitly NOT the F21 proof; the session watches via the public API for a NEW run id whose headSha is 9dfc71b or later and captures the three artifacts - F21 step result, /api/native-status credsspStatus (+credsspWhy), and the cert chain fields. §3 VERIFY LOOP armed, not guessed: user ticks Drives/Clipboard/Printers -> Connect; success => RDP USAGE ticks => only then may the "CREDSSP LIVE: handshake=ok, cert=trusted, policy=aligned, fullscreen=yes, usage=ticking" line be written. On failure (0x904/0x7, TcpStateFrontAuth) collect client CONNECTION DIAGNOSTICS copy-lines (CredSSP Parameters reg, LmCompatibilityLevel, `mstsc /v:<fqdn> /admin /prompt`, ClientActiveXCore/Operational .evtx export) plus runner credsspStatus + Security 4624/4625 + System Schannel + the cert Verify() result, map the EXACT mismatch, then land exactly ONE targeted fix - A CredSSP policy align (server AllowEncryptionOracle = client expectation, strict preferred), B cert trust assert (LE chain; fail loud if a self-signed cert reaches the listener), C LmCompatibilityLevel align (server=3 unless the client proves otherwise) - via the §1 PR path, re-dispatch, max 3 loops, stop with log-mapped evidence. §5 refusals hold and are unchanged: no credential-UI automation primitives, no NLA/CredSSP weakening (alignment only), no cert-validation bypass, no Defender/SAC/WDAC changes, no direct main push from cloud, no public exposure. §6 ZERO-CLAIM STOP: no LIVE line and no setup change landed in this phase - the phase's entire remaining work is the two user actions above (dispatch the lane, then run the click-through), and any fix they imply stays pending until log-mapped.
- F20 KILL THE nslookup FALSE ALARM + EVIDENCE-BASED 0x904/0x7 FALLBACK (session branch arena/01a0dd07-supreme-lamp; commits 77e0d20+35706de+b7e8fe4; PR #43: gates + windows-native + proof ALL GREEN; lab matrix 16/16 incl. the new cell t; verify loop PENDING-USER): GROUND TRUTH (log-verified) - client DNS is FIXED (ping + Test-NetConnection + the dashboard in-page fetch-probe all resolve the runner FQDN), every runner probe is green (listening/termService/fw 100.64.0.0/10/cert/nla) and the launcher beacon says ok=true, so the only DNS "failure" left was the DIAGNOSTIC itself: nslookup bypasses the Windows DNS client service (and therefore Tailscale NRPT split-DNS) and queries the router fe80::1 directly, so its timeout is EXPECTED and not a fault. §1 ui.html CONNECTION DIAGNOSTICS: the DNS copy-line is now `Resolve-DnsName <fqdn>` (id connDiagRd, wired to the live FQDN; the nslookup copy-line and its connDiagNs id are DELETED) plus the note "nslookup bypasses Tailscale split-DNS and queries your router directly; a nslookup timeout while ping works is EXPECTED and not a fault."; `ping <fqdn>` + `Test-NetConnection <fqdn> -Port 3389` are unchanged (F18/F19 pins moved connDiagNs -> connDiagRd). §2 the row gains a COLLAPSED `<details id="connDiagFailBox">` "if mstsc still fails (0x904/0x7) - export the logs" block - copy-only, one copy control per line, five wevtutil exports: client `wevtutil epl Microsoft-Windows-TerminalServices-ClientActiveXCore/Operational %TEMP%\rdp-client.evtx` + `wevtutil el | findstr /i "terminal credssp schannel"`; runner `wevtutil epl Security %TEMP%\rdp-security.evtx /q:"*[System[(EventID=4624 or EventID=4625)]]"` + `wevtutil epl System %TEMP%\rdp-system.evtx /q:"*[System[Provider[@Name='Schannel']]]"` + `wevtutil epl Microsoft-Windows-TerminalServices-RemoteConnectionManager/Operational %TEMP%\rdp-termgr.evtx`; the instruction is "Paste the exports into this session" and map the error from the logged event (cert chain / CredSSP / NLA / transport) - never guessed, and nothing suggested weakens NLA, CredSSP or certificate validation. §2 LAUNCHER 2.2.0.0 -> 2.3.0.0 (stamp "(F20 evidence-fallback+nslookup-kill)", assembly + AssemblyFileVersion bumped so the F19 version guard tells installed clients to re-run install.cmd): an mstsc that exits within 2s is logged `mstsc exited within 2s, code=<dec> (0x<hex>)` (hex because mstsc reports the 0x904/0x7 class in hex) and the single MessageBox now reads "mstsc exit code <dec> (0x<hex>) ... see dashboard -> CONNECTION DIAGNOSTICS -> \"if mstsc still fails\" and send the exports ... last 5 log lines"; fail-visible/visible-window contracts untouched (still exactly one MessageBox.Show, no CreateNoWindow/WindowStyle.Hidden). §3 launch-gates `F20 nslookup false alarm killed + evidence-based 0x904 fallback`: FAILS if ui.html offers nslookup in a copy-line (`<code ...>nslookup`) or keeps id=connDiagNs, pins Resolve-DnsName + its live-FQDN wiring + the false-alarm note + ping/TNC, FAILS if the block is missing/un-collapsed/outside the row/lacks any of the five exports or the paste instruction, if a non-copy control or an exec token (powershell/mshta/wscript/cscript/iex) appears, or if NLA-CredSSP-cert weakening advice is suggested; pins the launcher exit-code log + dashboard pointer + Ver>=2.3.0.0 (sort -V) and re-asserts the standing bans (no credential-UI automation, no creds in a ghrdp:// URL, no script-host launch from the page). §4 PROOF: tests/f20-evidence-fallback.test.js EXECUTES the shipped renderer (the real nativeStatus() out of payloads/ui.html under a DOM stub) and asserts the RENDERED copy-lines, the collapsed copy-only block with one copy control per export line, and the launcher dialog/exit-code contract; 137/137 node tests, 19/19 launch-gate bash blocks; the F20 gate is mutation-tested NON-VACUOUS (8/8 faithful mutations caught: nslookup copy-line restored, connDiagNs restored, note deleted, block un-collapsed, RemoteConnectionManager export dropped, non-copy control injected, dialog pointer removed, version rolled back). LAB (ghrdp-lab still 404 => push-triggered autologin-lab fallback lane, run 36232790408, ALL 21 steps success): cell S no longer hand-types the launcher constant - it READS `private const string Ver` + the Stamp suffix out of payloads/ghrdp-rdp-launcher.cs (a hand-typed 2.2.0.0 would have gone stale at this bump) - and new cell T proves the corrected diagnostics against the SHIPPED files (nslookup is no copy-line + Resolve-DnsName/false-alarm note wired, collapsed block carries all five exports and stays copy-only, SHIPPED launcher compiles with the in-box csc and its dialog points at the block); matrix k2..s all pass plus the new t=pass, and the notices publish "[T] PASS - F20 ..." + "[S] PASS - ... repo constant=>current ...". The prior F20 attempt (PR #42, branch arena/01a0dcee) was CONSOLIDATED here and closed: its content is identical byte-for-byte on the F20-touched files, but its lab run was RED in an unrelated cell (I) and its cell S hand-typed the constant; cell I passed in this run, so the failure was flaky, not a regression. §5 VERIFY (PENDING-USER): WINDOWS AUTO-LOGIN -> tick Drives/Clipboard/Printers -> Connect -> fullscreen -> RDP USAGE ticking; no "SYSTEM LIVE" line until the user confirms a fullscreen session. If 0x904 returns: collect the §2 exports, map the cause from the logs (cert chain / CredSSP / MTU), ONE targeted fix, max 3 loops, stop with evidence.
- F19 CLIENT-DNS SELF-DIAGNOSIS + LAUNCHER VERSION GUARD (this session; session branch arena/01a0dca8-supreme-lamp; local gates (18/18 bash blocks incl. the F19 block) + 130 node tests green; CI/lab verify on push pending): GROUND TRUTH (log-verified) - the launcher chain works (cmdkey stored -> 19-directive .rdp, no credential lines -> mstsc started) and the Security dialog already shows "Use the following credentials: Password for <user>", so the 0x904/0x7 class is NOT a credential problem: F17 proved the CLIENT cannot resolve *.ts.net while the IP-based dashboard works (client Tailscale DNS acceptance off), and the installed exe was still the F15/F17 build (no F19 guard on the client). §1 ui.html IN-PAGE CLIENT-DNS PROBE (zero native code): on load + every 30s two bounded (3s AbortController) `mode:'no-cors'` fetches - A=http://<runnerResolvedIP>:7331/, B=http://<fqdn>:7331/; pure core `clientDnsCore(fqdn,ip,scheme,aOk,bOk)` between the `[F19 §1 client-DNS probe-begin/end]` markers => RED card "YOUR PC's Tailscale DNS is off (name not resolvable, IP reachable)" with the copy-only fix lines `tailscale set --accept-dns=true` / `ipconfig /flushdns` / `ping <fqdn>` + tray path (Tailscale -> Use Tailscale DNS), GREEN "client DNS ok" when B answers, honest 'unreachable' when neither answers and 'skipped' under https (mixed content); no buttons, copy-only like the F17 diagnostics. §2 LAUNCHER VERSION GUARD: launcher v2.2.0.0 (`private const string Ver = "2.2.0.0"` = the REPO CONSTANT) stamps every beacon (`exe` field, version string only); main.yml stage step READS the constant out of payloads/ghrdp-rdp-launcher.cs (regex, fail-closed `::error::[F19]` + throw when missing - never hand-typed) into config.launcherVersion; ghrdp-server.ps1 extracts the version from the beacon store and compares `[version]$launcherSeenVersion -lt [version]$launcherVersion` => `/api/native-status` +launcherVersion +launcherSeenVersion +launcherOutdated (+lastHandlerVerb.exe); ui.html yellow row "launcher outdated - re-run install.cmd once (DOWNLOAD INSTALL KIT)" - yellow ONLY, never an AUTO-LOGIN blocker (the F17 gate `var all=ok&&lOK&&dOK;` is unchanged and the row is deliberately excluded from it). §2 LAUNCHER (recompiled by install.cmd): ghrdp://rdp now carries `&ip=<tailnet-ipv4>` (an address is NOT a credential; CGNAT-validated, diagnosis ONLY - `MstscStep(server,...)` still targets the FQDN, never the IP, and no .rdp line carries it); DnsDecision(dnsReason,ip,ipReachable) => 'mstsc' | 'client-dns-off' | 'dns-fail'; on fail-name + `IpReachable(ip,3389)` the MessageBox is "Your Tailscale DNS is off. Run once: tailscale set --accept-dns=true (or tray -> Use Tailscale DNS), then ipconfig /flushdns, then retry." with beacon ok:false details='client-dns-off' (rc5, mstsc NOT started - the F17 box remains the fallback); a credential-ish URL param (pass|password|passwd|pwd|token|key|secret|apikey|authkey|cred) is REFUSED before any work (details='cred-param-rejected', rc6) and its value is redacted from the log; `--dns-selftest` (lab-only, unreachable from a URI) runs the 5-case decision matrix with injected resolver/TCP results and never launches mstsc/cmdkey. §3 docs/MIGRATION.md §1.11 documents the OPTIONAL FUTURE code-signed .rdp (self-signed CodeSigning cert + one-time client trust + rdpsign) as NOT IMPLEMENTED - the F19 gate fails if any shipped code names a signing/trust API. §4 launch-gates `F19 client-DNS probe + launcher version guard + no-credential-automation gates` + a `windows-native` step that COMPILES the shipped launcher with the in-box csc and asserts the matrix; AE1 bans the keyboard/UI-automation API identifiers repo-wide (the gate block itself is the only place that names them; comments included - verify-ghrdp.ps1's dialog scan was rewritten to a READ-ONLY window-title scan, no UI-automation library at all), AE2 bans an injection primitive within 40 chars of a credential target (password auto-type patterns) while still allowing human copy like "type the password at the Windows prompt", AE3 keeps the one legit SendInput user (WEB DESKTOP remote control) credential- and cmdkey-free; a ghrdp:// URL cannot carry a credential parameter (single builder + launcher refusal). §5 lab/CI proof (ghrdp-lab still 404 => local + fallback lane): tests/f19-dns-launcher.test.js 13 tests EXECUTE the shipped probe core (extracted verbatim) over the full A/B matrix (A-ok/B-fail => client-dns-off, B-ok => ok, both-fail => unreachable, no-ip/https => unknown/skipped) + pin the launcher guard, the single credential-free URL builder and the version-compare semantics; autologin-lab cell S compiles the SHIPPED launcher and asserts the injected matrix (verdict=pass, 0 mstsc/cmdkey) and drives the REAL server with an old (2.1.0.0 => launcherOutdated=true) and a current (2.2.0.0 => false) beacon; gate block mutation-tested non-vacuous (probe rename, probe period, verdict string, builder credential, outdated-row wiring, version-compare loosening, hand-typed constant, MIGRATION note removal all caught). ALSO FIXED: the F18 gate's awk escape (8-vs-1 backslash) that made launch-gates RED on main (run 36226408368) - F18 + F19 blocks now green locally. §6 physical verify (one-time client DNS command + install.cmd => dashboard "client DNS ok" + launcher current => AUTO-LOGIN => fullscreen => RDP USAGE ticking) PENDING-USER; if 0x904 persists after DNS ok, read the runner rdpListener fields + TerminalServices events (max 3 loops). NO 'CLIENT-DNS LIVE' claim until the user confirms. POST-MERGE (PR #37 -> main 3d2b6e6): the three PR checks are green (gates / windows-native / proof) and the lab matrix is 15/15 (k2,l,m,n,o,a,b_c,d_e,f,h,i,j,p,r,s all pass; cell S publishes the 'F19 ... shipped launcher DNS-guard matrix 5/5 ... (cell s)' PASS notice). Two PRE-EXISTING lab defects had to be fixed before that proof could run: cell R asserted a 'dns-guard ok' log line the launcher never wrote, so R (step 7) silently skipped every later cell - S included - and the lab had been red since F17 (r=fail in main's run 36226408423 too); the launcher's success path now logs 'DNS resolved <fqdn> -> <tailnet ips> (dns-guard ok)' ('DNS resolved ' stays pinned by the F18 gates). Also: the report's ::notice:: evidence lines carried the achievement even for cells that failed or were skipped, so the report could read green while the matrix said fail - each notice now says 'PASS - ...' or 'NOT VERIFIED (cell did not run) - ...'; the winexe selftest is waited on with Start-Process -PassThru + $p.WaitForExit() (F10-9 keeps -Wait banned, and the F19 gate pins the explicit wait); the assertions use String.Contains because the '[F19 §1 client-DNS probe-begin]' marker's brackets are wildcard metacharacters (WildcardPatternException had killed the step). ghrdp-lab re-checked: still 404, so §5 ran in the reserved-lab fallback lane.
- F17 RDP 0x904/0x7 DIAGNOSIS + SELF-REPORTING LISTENER (this session; commits 5a1a99f..ae4305a; local gates+113 node tests green, lab/CI verify on push pending): ROOT CAUSES - (a) client PC could not resolve the MagicDNS FQDN at all (ping/TNC name-resolution failure), so mstsc died 0x904/0x7 before any credential dialog; (b) beacon age +05:30 (19805s): ConvertFrom-Json converts the ISO-Z ts to a SERVER-local [datetime] and `[string]$lv.ts` re-serializes it WITHOUT Z, which the visitor's browser parsed as LOCAL (+05:30 => exactly +19800s); (c) ghrdp-server.ps1 carried a stray `ry {` parse-corruption line after listener.Stop (5a1a99f). §1 main.yml step `RDP listener self-probe (F17)` after firewall+cert+stage: probes listening (TNC 127.0.0.1:3389), termService, fwRule ghrdp-rdp-3389 (enabled+ALL profiles+scope EXACTLY 100.64.0.0/10, missing/wrong self-heals in place - never unscoped), certThumb (SSLCertificateSHA1Hash present + cert valid + DNSName==config.dnsName), nla (UserAuthentication==1); writes config.json rdpListener BEFORE any loud fail so a red run still publishes the exact ❌ field, then `::error::[F17]` + summary + throw (listening=false | fwRule wrong | certThumb invalid => run never advertises a dead host). §2 server: Get-RawJsonTs (raw-text ts extraction - ConvertFrom-Json datetime conversion is the bug) + ConvertTo-UtcDateTime/Get-UtcAgeSeconds/ConvertTo-UtcIso (DateTime.Parse RoundtripKind; bare ts = UTC, never local); beacon/launcher-status/rdp-usage/rdp-ping ages + the lastHandlerVerb.ts echo (now explicit UTC ISO-Z) all rewired; beacon store (handler-hello-last.json + launcher-hello-last.json) RESET at server start = per-run rows; /api/native-status +rdpListener +rdpListenerAgeSec. §2 ui.html: parseTsUtc (explicit Z/offset honored, bare ts read as UTC - TZ-sensitive node test proves the +05:30 fix under TZ=Asia/Kolkata), paintBeacon + hello-watch rewired; RDP LISTENER row (listening/termService/fw/cert/nla ✅❌ each with exact fix text + probe age); WINDOWS AUTO-LOGIN disabled unless listening+fw+cert+nla ALL ✅ (missing probe = disabled, honest note); CONNECTION diagnostics copy-only lines `ping <fqdn>` + `Test-NetConnection <fqdn> -Port 3389` with the name-resolution-vs-listener guidance. §3 launcher v2.1.0.0 (F17 dns-guard): DnsGuardReason BEFORE cmdkey/mstsc - resolver failure/zero addresses/non-tailnet address (not 100.64.0.0/10 v4, not fd7a:115c:a1e0::/48 v6) => JSONL `dns-guard blocked launch` + beacon ok:false `dns-guard: ...` + MessageBox "DNS stale/blocked - flushdns or check Tailscale ... mstsc was NOT started" rc5; GHRDP_LAB_DNS_ALLOW_LOOPBACK=1 is the documented LAB-ONLY relaxation (hosts-file loopback alias on headless runners), set at autologin-lab job env, NEVER in main.yml (gate-enforced). §4 launch-gates `F17 RDP listener probe + client-DNS guard gates` (probe step + loud-fail present + write-before-fail order; RemoteAddress/remoteip scope strings == 100.64.0.0/10 everywhere in main.yml+Provision-GhrdpVps.ps1, 0.0.0.0/0 only in # comments; RoundtripKind + raw-ts + reset + rdpListener in server; parseTsUtc + RDP LISTENER + __listenerOk gate + connDiag ids + exact TNC line in ui; DnsGuardReason call before CmdkeyStep call; lab switches lab-only) - mutation-tested non-vacuous (scope drift, parse rename, lab leak, loud-fail removal, probe gutting all caught); tests/f17-listener.test.js 9 tests. §5 lab (ghrdp-lab STILL 404 => autologin-lab cell R on push incl. main.yml path): baseline mirrors production enablement (3389 listen + NLA + scoped rule + self-signed cert for the lab FQDN), EXTRACTS the §1 probe VERBATIM from main.yml between the probe-begin/probe-end markers and proves (1) baseline all-✅ fwScope=100.64.0.0/10, (2) rule disabled => rc!=0 + ::error::[F17] + fwRule=false with listening still true, (3) re-enable => all-✅, (4) scope drift 10.0.0.0/8 self-heals to exactly 100.64.0.0/10 fwFixed=true, (5) REAL server 7392 native-status renders rdpListener + age 0-120s, (6) beacon age <60s right after a check verb + lastHandlerVerb.ts ends Z, (7) REAL launcher vs public-IP hosts alias => rc5 dns-guard no cmdkey/mstsc, (8) loopback alias + lab switch => dns-guard ok + mstsc-started; matrix +r=; all prior cells/gates kept. §6 physical verify (dispatch => dashboard RDP LISTENER all ✅ => AUTO-LOGIN => fullscreen => RDP USAGE ticks) PENDING-USER.
- F15 FAIL-VISIBLE LAUNCHER + BEACON DIAGNOSTICS (this session; 9 commits 85e3203..782f0ab; PR #32 MERGED as 2b654f0; gates+windows-native+proof green, fallback lab lane green; PHYSICAL VERIFY PENDING-USER): §1 `payloads/ghrdp-rdp-launcher.cs` rewritten fail-visible - FIRST instruction = JSONL line + `POST /api/handler-hello {verb,ok:true,details:"invoked"}` before ANY work (beacon host clamped to a validated *.ts.net FQDN so a stray URI cannot POST anywhere); interactive cmdkey in a VISIBLE Normal window (`UseShellExecute=true`, `WorkingDirectory=SystemDirectory`, the valueless `/pass` form, `WaitForExit(180000)` fixed in production / lab-shortenable only, then `/list` verify -> `cmdkey-shown`, `cmdkey-stored=true|false`, or `cmdkey-timeout` rc1) with NO CreateNoWindow/WindowStyle.Hidden anywhere; local fullscreen `.rdp` (no credential line, no MOTW); VISIBLE mstsc + `WaitForExit(2000)` -> `mstsc-started pid=N`, else MessageBox(exit code + last 5 log lines) rc4; global catch -> logged MessageBox + beacon `ok:false details=<ExType>` rc1; `ghrdp://check` self test MessageBox (registered command, TERMSRV presence, log path, version stamp) rc0 and NO beacon without a server arg; JSONL `%LOCALAPPDATA%\ghrdp\ghrdp-launcher.log` with password/token redaction. §2 `install.cmd` v2 runs the `ghrdp://check` self test after reg add (the MessageBox IS the install-time proof) + prints the log path/Defender-policy hint. §3 ui.html launcher-beacon row (verb/details/ok/age), 45s `invoked`-without-`mstsc-started` red STALL row naming the log path, [RUN CHECK]. §4 launch-gates F15 block (hidden-flag adjacency including mere mentions, hello-before-work inside Main, exactly one logged `MessageBox.Show`, install self-test, UI/stall/RUN CHECK tokens) + `tests/f15-fail-visible.test.js` (104 local tests green; F12/F14 gates kept). §5 lab (ghrdp-lab still 404 => fallback lane): A compiles the SHIPPED install.cmd with in-box csc and proves the self test really ran; P (python hello-listener on its own port 7391 via the launcher's port arg, hosts alias, REAL launcher) proves check-no-server posts ZERO beacons, `check-shown` with a server, stored-cred `invoked -> cmdkey-stored=true -> mstsc-started pid=N`, no-cred `invoked -> cmdkey-shown -> cmdkey-timeout ok:false rc1` with mstsc NOT started, and 26 JSONL lines / 4 logged dialogs / zero credential-looking tokens; matrix k2=l=m=n=o=a=b_c=d_e=f=h=i=j=p=pass on 782f0ab. §6 physical-PC click-through PENDING-USER (first click = visible cmdkey prompt then fullscreen, later clicks silent; any silent exit -> log + stall row); the §8 LAUNCHER FAIL-VISIBLE line is NOT claimed.
- F14 ONE-CLICK INSTALL-KIT DOWNLOAD (this session; 7 commits 8205da4..54fbdc8; CI green, physical verify PENDING-USER): §1 `payloads/install.cmd` replaced byte-faithful (quoted-set vars, EXE=`ghrdp-rdp-launcher.exe`, winexe + System/Forms refs, BEFORE/AFTER printed, no script-host invocation - the mandated DONE line prints "NOT powershell.", the F14 gate bans invocation forms only); main.yml `Build ghrdp-handler-kit.zip (F14)` step Compress-Archives EXACTLY the 2 payloads into the stage dir (entry-count readback fail-closed) so stage->root copy lands kit+payloads in C:\ghrdp. §2 `ghrdp-server.ps1` GET /dl/<name>: `$script:DlNames` allowlist EXACTLY {ghrdp-handler-kit.zip, install.cmd, ghrdp-rdp-launcher.cs}, tailnet-only (CGNAT 100.64/10 + loopback for the lab), NO dash token, Content-Disposition attachment (single-buffer head+body write after lab run 36210404270 curl(8) Nul-byte-in-header), no-store (global header), /dl + /dl/ + every non-allowlisted name 404. §3 ui.html handler row: always-rendered [DOWNLOAD INSTALL KIT] anchor -> /dl/ghrdp-handler-kit.zip + Path A ordered guide 1-6 + Path B (repo clone = zero MOTW warnings); WEB DESKTOP primary + winAutoStale untouched. §4 launch-gates `F14 one-click kit gates` (allowlist count==3 + no secrets-bearing name, install.cmd invocation ban, kit step lists exactly 2 payloads, on-page href/guide markers, WEB DESKTOP primary kept); F12 §1.1 token greps re-pinned to the new text; all prior steps kept; tests f10/f12 re-pinned (96 node tests green). §5 lab: ghrdp-lab STILL 404 => autologin-lab cell O (REAL server: raw-socket downloads, 3/3 sha-identical vs source + kit members, attachment+no-store, 5 decoys + 2 bare /dl = 404, page renders href+guide) + f14-diag artifact; GREEN on 36210704491 (o=pass, full matrix pass) + launch-gates 36210704349 PASS. §6 physical-PC verify (click -> extract -> install.cmd -> Run once -> BEFORE/AFTER -> first-time password -> silent second click) PENDING-USER - no LIVE claim.
- F12 CLOSE-OUT (prior session; superseded only in install.cmd wording by F14): (a) §1 stale-handler overwrite + PS-free auto-login: `payloads/install.cmd` prints the ghrdp handler value BEFORE, overwrites ANY prior HKCU value (the pre-F2 `powershell.exe` registration behind the client's "Open Windows PowerShell?" prompt), prints AFTER and verifies the readback; `payloads/ui.html` watches the launcher's `/api/handler-hello` beacon for 20s after a WINDOWS AUTO-LOGIN click (3s native-status poll, `__helloWatchArmedAt`/`lastHandlerVerb`) and, when no fresh beacon arrives, shows the stale-registration notice + the copy-once install text. (b) §2 cert-bind regression: the DNS gate (`Wait for Tailscale connected`) exports `TS_MAGICDNS_FQDN`; `Bind tailnet LE cert` refuses to run without it (ordering proof), retries `tailscale cert` 3x10s, and EVERY failure path is fail-closed via `reason=cert-not-bound` + summary + throw - exactly ONE `exit 0` survives (the success path). (c) §3 all-browser + qBittorrent: Firefox force-install resolves id+xpi live from the AMO API (`Resolve-AmoAddon`); Edge/Chrome forcelist readback via `payloads/ghrdp-provision-apps.ps1`; qBittorrent default handlers for `.torrent` + `magnet:` (advisory, plain cached winget app). (d) §4 VNC memory: shim retries 5x500ms then purges, opener+exact-origin gating, launch URLs never carry `password=`. (e) §5/§6 `PreferZlib`; hidden/SYSTEM helper contract kept. Lab cells K/L/M/N; `dekarita/ghrdp-lab` unreachable (404) so lab-green-before-PR was NOT satisfiable then - physical-PC cells stayed PENDING. Docs: `docs/AUTOLOGIN.md` 0.1/0.2/section 4.
- F10 PS-free auto-login package (this session; supersedes nothing - it reuses F9n/F9o web desktop untouched): `payloads/ghrdp-rdp-launcher.cs` (C#5, in-box csc via `payloads/install.cmd`, HKCU `ghrdp` = `"<exe>" "%1"`, verb `ghrdp://rdp?server=<fqdn>&user=<u>`: interactive cmdkey once, writes a LOCAL `%TEMP%\ghrdp-<sha1_8>.rdp` - fullscreen `screen mode id:i:2`, all nine redirect directives, bandwidth autodetect, NO password/hash, NO desktopwidth/height pins - starts mstsc visible, deletes the temp file after mstsc holds it, POSTs a bounded handler-hello beacon); `payloads/edge-ext-resolve.ps1` (one shared LIVE extension-ID resolver, dot-sourced by main.yml AND autologin-lab.yml: discovery from store search/vendor/SERP pages then detail-page publisher-marker verification, ids never hardcoded, unresolvable = skipped loudly); server logon-age (`Win32_LogonSession` LogonType 10) + real ping (`tailscale ping` every 15s to the dash-token source peer -> rdp-ping.json -> pingMs/pingPath) + dash-token-gated `creds` block (masked last-4 + copy; loopback never); ui.html WINDOWS AUTO-LOGIN primary button + KEYS creds rows + logon-age ticker + relay advisory; TightVNC poll/CompareFB tuning + noVNC `compression=6`; setup caches (MSI/noVNC/pip/cargo) + 10 stage steps merged + Pages PUT skipped when docs unchanged; autologin-lab proof lane (A-E, F, G cells + de-diag artifact + annotation-only evidence).
- F10 lab/hardening fixes (this session, one commit each): F10-9 lab harness `-PassThru -Wait` removed (it waited for the mstsc descendant and wedged run 36147990362 in step B/C -> bounded `WaitForExit(90000)` + reap); F10-10 launcher handler-hello bounded (background thread, 5s join; `ReadWriteTimeout`, `KeepAlive=false`, `Proxy=null`) so telemetry can never wedge the launcher; F10-13 lab D/E classifies every failure (`::error::[D/E] stage=<x>`) + uploads a de-diag artifact (runner logs are unreachable from the CI token); F10-14 lab D/E loopback calls bypass any proxy (raw HttpWebRequest, `Proxy=$null`) and print HTTP status + body; F10-15 server `Test-IsLoopbackAddr` (the `$ip.IsLoopback` *property* is null in PowerShell, so genuine 127.0.0.1 clients were 401'd - static API + 127.0.0.0/8 bytes now); F10-16 creds mask fixed (the old one-liner compared `[string]$v.Length -le 4` as strings, so every password rendered `****`); F10-17/18 resolver per-source warning diagnostics + store classification/preference. LAB GREEN on `ca99237` (run 36151203130: A, B/C, D/E, F, G all success) - physical-PC cells stay PENDING-USER.
- F10 RDP UX + PS-free auto-login redirect (this session; PR pending): ghrdp://rdp?server=<fqdn>&user=<u> launcher in payloads/ghrdp-rdp-launcher.cs (C#5) + install.cmd (in-box csc, HKCU, no script host/UAC; interactive cmdkey ONCE when TERMSRV/<fqdn> absent - tooling never sees the password; local no-MOTW temp .rdp, fullscreen=client native, 9 redirect directives, no password/hash/resolution lines, NLA/CredSSP defaults untouched, best-effort rdp delete + /api/handler-hello). ui.html: primary WINDOWS AUTO-LOGIN + copy-once install hint + WEB DESKTOP zero path; KEYS rows (masked last-4 + copy) fed by the dash-token/tailnet-gated /api/config creds block (Test-CredsAllowed; loopback excluded; vncPass now stripped by Remove-CredKeys). Server: rdpLogonAgeSec (Win32_LogonSession LogonType=10 newest StartTime), rdp-ping 15s loop (tailscale ping -c1 -timeout 3s vs dash-token SOURCE PEER via RemoteEndPoint/X-Forwarded-For capture) -> pingMs/pingPath; CONNECTIVITY row + UDP-41641 relay advisory; webdesk opens vnc.html?autoconnect=true&compression=6; TightVNC Poll*/CompareFB registry tuned. Edge: ExtensionInstallForcelist IDs live-resolved at build time from Edge Add-ons pages (publisher marker; wrong pre-F10 Chrome Dark Reader id dropped, CWS ids title-verified); managed bookmarks = {Mission Control, docs/AUTOLOGIN.md, TS admin DNS}; piracy-index strings scrubbed + gate. Setup time: actions/cache (MSI/noVNC/pip/cargo), 10 stage steps merged, MSI download parallel job, Pages PUT skipped when docs bytes identical; cert-bind kept sequenced (same-job config locality, in-step parallelism only where independent). Gates+tests: launch-gates F10 block + tests/f10-autologin.test.js (7 files green locally). Lab: ghrdp-lab STILL 403/404 (retried) -> autologin-lab.yml fallback; §6(a-c) proven at launcher/protocol level; physical-PC cells + §7 dispatch stay PENDING-USER - AUTO-LOGIN REDIRECT LIVE line NOT claimed.
- U1 repair (launch-gates run 36045997362 PASS; live client NOT verified): restored VPS-only AUTO-LOGIN button, explicit cmdkey confirmation, rid-only URI, validated WEB DESKTOP URL + linked missing-secret advisory, zero E-battery UI matches; server requires dashboard bearer for 60s single-use rid, returns only FQDN; pinned compiled .NET handler built and self-tested on Windows CI. User must install binary on client; no real RDP connection claimed.
- G3 native hardening (launch-gates run 36047443590 PASS; live VPS NOT verified): provisioning locks static rdpuser, NLA+TLS-only/CredSSP, exact-FQDN LE cert+key ACL, IPv4 tailnet-only TCP/3389, protected config+token and hostKind=vps; certBound checks exact name/expiry; missing VNC_PASS clears URL and halts, install failures withhold secret-bearing exception text. F9g stage DNS retry was already present. VPS dashboard/web gateway still require user deployment and live validation; do NOT decommission Actions.
- F9h main.yml web-desktop step: VNC_PASS missing is now FAIL-CLOSED. Guard runs BEFORE any install command; writes ::error:: annotation + Step Summary card with a direct link to /settings/secrets/actions, stamps config.json webdeskReason='vnc-pass-missing' + vncPassAdminUrl, then throws (run fails by design). Supersedes the old loud `exit 0` skip, which reported a green run with the web desktop permanently absent. Belt block retained (also throws) so the two F8 launch-gate markers stay in the step region.
- F9i web-desktop root-cause fix + fail-closed + honest UI (run 36050989748 post-mortem): the serve-failed runs came from `Start-Process python -m websockify -RedirectStandardOutput $wsLog -RedirectStandardError $wsLog` (SAME file; Windows exclusive file open makes Start-Process throw before python starts, so 7333 never bound while the step stayed green). Now two separate log files; all 4 startup-failure exits + self-test FAIL throw (fail-closed, no false-LIVE) with fixed secret-free webdeskDetail codes (tightvnc-install|novnc-assets|websockify-bind|serve-mapping|self-test-failed); serve mapping re-checked once after 5s settle. Short-but-present VNC_PASS now stamps 'vnc-pass-too-short' (never 'vnc-pass-missing'). /api/native-status exposes webdeskDetail + vncPassAdminUrl; ui.html renders guidance from the VERIFIED reason (vnc-pass-missing = secrets setup; serve-failed = host-side startup failure, explicitly NOT a missing secret; plus too-short/config-stale/step-not-run/invalid-webdesk-url). WEB DESKTOP stays primary, enabled only for a valid tailnet HTTPS URL. Tests: tests/webdesk-reasons.test.js + tests/workflow-webdesk.test.js (29 Node tests pass); live re-dispatch + Proof C still pending.
- F9j main.yml serve-mapping hardening (serve-failed run post-mortem, log showed no mapping + zero diagnostics): webdesk serve now (1) derives webdeskUrl only from a VERIFIED `serve status --json` mapping and normalizes it to the default port - current tailscale keys Web by '<host>.ts.net:<port>', so the old raw-key URL carried ':443' which ui.html's port-less validator rejects (green run, dead WEB DESKTOP button = false LIVE); (2) prints the serve exit code and retries once in explicit-443 form; (3) on failure prints secret-free diagnostics (serve log tail, serve status, tailscale state) before the fail-closed throw. Watcher websockify auto-heal rebinds 127.0.0.1:7333 with --web novnc (was all-interfaces 7333 - bypassed tailnet TLS). Tests: tests/workflow-webdesk.test.js F9j block (33 Node tests pass); launch-gates D/E/F8/F9 re-verified.
- F9k main.yml web-desktop serve syntax fix + fail-closed serve logic (serve-failed run post-mortem: 'Error: invalid argument format' from the array-splatted `tailscale serve` argument forms on the current windows-latest Tailscale CLI): PRIMARY form passes the explicit port mapping as ONE concatenated argument string ('--bg http://127.0.0.1:7333') - string concatenation, NOT array expansion, so PowerShell cannot re-split/re-quote the tokens for the native CLI; FALLBACK = explicit-443 bare-literal form ('--bg 443 http://127.0.0.1:7333'); both attempts print their exit code to the step log (F9j contract); a serve failing in BOTH forms falls through the single serve-mapping fail-closed block (secret-free diagnostics + 5s settle re-read + webdeskReason='serve-failed'/webdeskDetail='serve-mapping' stamp + throw - no duplicate reason writes). URL still derived only from a VERIFIED `serve status --json` mapping (no fabrication); VNC_PASS never passed to tailscale. 33 Node tests + D/E/F8/F9 gates re-verified locally. Live re-dispatch + Proof C still pending.
- F9l main.yml web-desktop serve cascade (this session; supersedes the F9k forms, both proven dead on the live runner - Tailscale 1.52+ removed the '<listen-port> <target>' positional syntax: run log exit 1 'invalid argument format' / exit 2 usage dump): the step logs the CLI version, then tries '--bg http://127.0.0.1:7333' (post-1.52 URL target, default https=443), '--bg --https=443 http://127.0.0.1:7333' (post-1.52 explicit listen port), '--bg 7333' (post-1.52 port-number target) and '--bg 443 http://127.0.0.1:7333' (pre-1.52 legacy, kept); after each attempt the mapping is read back via Get-WebdeskServeUrl and the first VERIFIED mapping wins; if none verifies, the unchanged single fail-closed serve-mapping block runs (one Set-WebdeskCfg '' 'serve-failed' 'serve-mapping' + throw). No URL invented; every attempt's exit code logged; CLI version logged for future diagnosis. main.yml ~:1263.
- F9m main.yml serve root-cause instrumentation + documented-form cascade (F9l live run, Tailscale 1.102.4: all four forms exit 1; the only captured text was 'Error: invalid argument format' - F9l reused ONE log file, so the first three attempts' output was lost): every attempt now captures+prints its own redacted stdout/stderr and appends it to the transcript the fail-closed diagnostics read; the CLI version and `serve --help` usage text are logged up-front; the cascade adds the documented '--yes' (non-interactive, CI has no TTY) and partial-URL ('localhost:7333') forms alongside URL/port/explicit-port, keeping the pre-1.52 legacy pair last (the v1.102.4 source's validateArgs proves that pair can only ever emit 'invalid argument format': exactly two positionals with the last not 'off' is invalid, and modern serve takes ONE target); if every form fails, a last-resort 'serve set-raw' attempt posts an ipn.ServeConfig JSON built from Self.DNSName and is then re-verified via Get-WebdeskServeUrl (a wrong schema yields no mapping - never a fabricated URL); the single fail-closed serve-mapping block, the 4 serve-failed writes and all security locks are unchanged. main.yml ~:1263.
- F9l(502) main.yml web-desktop 502 self-heal + survivable diagnostics (5 commits, one per fix; supersedes nothing - it reuses the F9m serve cascade untouched): F9l-1 idempotent `Start-Websockify` (kill stale python websockify on 7333, Start-Process with PAIRED redirect files, wait LISTEN on LocalAddress 127.0.0.1 + LocalPort 7333 specifically, return bool) defined byte-identically in the deploy step and the self-test step (gate-diffed); F9l-2 self-test rewritten as classified/self-healing - A direct backend curl http://127.0.0.1:7333/vnc.html (200+noVNC), B backend dead -> Start-Websockify -> re-probe, C advertised serve URL 3x5s capturing status+headers+first 200 bytes, D backend healthy BUT serve failing (502) -> `tailscale serve --bg http://127.0.0.1:7333` reset (+ --yes and legacy forms), 5s settle, re-curl 3x5s, E still failing -> classify backend-dead | proxy-502 | dns-tls, write classification + secret-free tails to Step Summary + `::error::` annotation, then clear webdeskUrl and throw; F9l-3 `webdesk-diag` artifact (websockify.log, websockify.err.log, serve-status.json, selftest-verbose.txt, classification.txt, MANIFEST.txt) staged INSIDE the workspace and uploaded with `if: always()` (upload-artifact v4 rejects absolute host paths - lab-proven), Step Summary names it; F9l-4 fail-closed contract locked (config keeps exactly serve-failed/self-test-failed; classification is diagnostic-only so ui.html messaging does not oscillate); F9l-5 non-vacuous launch gates (backend curl, launcher CALL, mapping reset, classification tokens, fail-closed throw, byte-identical launcher copies, artifact staged/uploaded with if: always() and workspace-relative path) - all 5 gate steps pass locally and each of 7 negative mutations makes the gate exit non-zero. Green/halt paths emit ::notice::/::error:: annotations so outcomes are readable from the Checks API without downloading runner logs. Follow-up commit 3830bc0 removes `-WindowStyle Hidden` from all production websockify launch paths (deploy, self-test, watcher auto-heal) and lab reproduction; LAB_REV=4 run 36099001127 passed on this SHA, preserving the 502 reproduction (dead process/no listener/backend 000/serve 502 with mapping), extracted production self-heal (backend + advertised URL 200/noVNC and listener alive in next step), and fail-closed dns-tls proof (backend 200, mapping present, config reason/detail locked). `webdesk-diag` uploaded (3,129 bytes); launch-gates + windows-native run 36099001162 passed. Node test enforces no hidden websockify windows. main.yml ~:1223 (deploy) / ~:1530 (self-test) / ~:1795 (diag steps).
- Lab fallback (this session): `dekarita/ghrdp-lab` is unreachable from CI tooling (gh repo view -> 404; gh repo create -> 403 "Resource not accessible by integration" - intent stated once + retried once). Equally faithful fallback added as `.github/workflows/webdesk-lab.yml` (push-triggered on its own file, windows-latest, REAL TS_AUTHKEY login + REAL `tailscale serve`, noVNC+websockify seeded, then the F9l-2 self-test EXTRACTED VERBATIM from main.yml and executed with `pwsh -File`, plus a Phase-3 classification proof against an unresolvable ts.net name). Phase 1 (commit 16328c5) reproduced the exact failing posture and its upload failure fixed the production artifact design. Delete this file when ghrdp-lab becomes available.
- F9k-secrets fail-closed framework (this session): main.yml 'Tailscale up' now FAIL-CLOSED on TS_AUTHKEY. Emit-SecretHalt(What,AdminUrl,Reason,Steps,Detail) writes ::error:: + a step-summary card (CLICKABLE admin-keys link + the fixed 5-step regen procedure: admin/settings/keys -> Generate auth key w/ Reusable+90d -> copy tskey-auth-... -> set repo secret TS_AUTHKEY -> re-dispatch) + config.json tsReason/tsAuthAdminUrl, redacts tskey-*/ghp_*/github_pat_*/raw secret values from every printed line, then throws. Missing key => ts-authkey-missing halt; the old interactive 'no sign-in link' fallback path is DELETED (fail-open). Rejected key: paired ts-up.log/ts-up.err, status --json poll <=90s with early fatal break, ONE 10s retry for transient control-plane errors only; last-25-line tail classified invalid|expired|revoked|not a valid => ts-authkey-invalid / rate|limit => ts-authkey-ratelimited / else ts-authkey-unknown; halt carries link+steps+redacted tail. 3x '# [F9k-guard]' cascade guards (one per ghrdp-lib.ps1 dot-source: stage/expose/keepalive) point at the FIRST failed step. ghrdp-server.ps1 /api/native-status +tsReason +tsAuthAdminUrl (allowlist-validated https URL). ui.html: second conditional yellow box (VNC_PASS pattern) for ts-authkey-missing|-invalid - clickable keys link + 5 steps + secrets-page link; a ts-authkey-* value in webdeskReason also lights it. launch-gates +F9k gate (keys link present, secrets-settings link present, guard-count == dot-source-count, non-vacuous); apply-patch.yml added (workflow_dispatch branch+patch_b64, git am --3way, push -u, gh pr create || true). tests/ts-framework.test.js => 43 Node tests pass; all gates simulated locally pass. F8/F9h VNC_PASS guard untouched. Live Run A (dead key must halt at 'Tailscale up' w/ ts-authkey-invalid) pending user re-dispatch; Run B + Proof C still required after key regeneration.
- F9f launch-gates: admin-link count >= 3 in main.yml + every Self.DNSName step must validate *.ts.net (no gate passes on empty/non-ts.net name); D/E + F8 gates kept untouched.
- F9a main.yml: all 3 MagicDNS gates (ts-connect, cert-bind, stage) append 'halted by design' + [Enable MagicDNS now](admin/dns) + re-run line to GITHUB_STEP_SUMMARY, echo to log, throw with URL; 15-min HOLDs removed (halt immediate; re-dispatch after enabling).
- F9b main.yml: opt-in auto-enable - TS_API_TOKEN+TS_TAILNET_NAME env (secrets) -> POST api.tailscale.com/api/v2/tailnet/<name>/dns/preferences {"magicDNSEnabled":true} (Bearer header, token never printed), sleep 5, re-read Self.DNSName; secrets absent -> silent skip.
- F9c ghrdp-server.ps1 /api/native-status: +magicDnsAdminUrl field; probeReasons.fqdn text carries the admin URL (feeds the fqdn reasonsDisabled rendering).
- F9d ui.html: fqdn reason linkified via <a target=_blank rel=noopener> (uses s.magicDnsAdminUrl); ephemeral+fqdn-missing yellow advisory row 'workflow halted until MagicDNS enabled - open the admin link, enable, re-dispatch'; row hidden once fqdn ok.
- F9e MIGRATION.md sec 1.3+1.7: admin link, DNS->MagicDNS toggle path (ON, Save, re-run), optional API one-liner (TS_API_TOKEN+TS_TAILNET_NAME), halt-by-design statement.
- Prior: 4812cdd5 remediation merged; 478d015b P1-P3 (cert script, FQDN discipline, POST-token/mutex/JSONL); 2252263e G3 provision; 053894c5 MIGRATION rewrite.
- Prior: U1-U4 native-mstsc rewrite (ui/server/helper, AUTO-LOGIN contract); F6-F8 certBound honesty, hostKind reasons, advisory rows, webdesk honesty + self-test; PR#9 F9 HOLD variant superseded by F9a halt-by-design.

- F11 FINAL UX PACKAGE (this session; 4 commits, supersedes nothing structural): §2 noVNC password memory - `payloads/ghrdp-cred-shim.js` deployed INTO the served noVNC copy by main.yml (verbatim copy + `<meta name="ghrdp-cred-origin" content="http://<rdpIp>:7331">` + one `<script src>` before `</body>`, idempotent); the shim accepts `postMessage({type:'ghrdp-vnc-pass',pass})` ONLY when source === window.opener AND origin === the exact gated dashboard origin, auto-submits the Credentials dialog (RFB.sendCredentials, else #noVNC_password_input + dialog button), purges the variable and drops its own opener; it contains no console/log/alert/document.title call. ui.html: VNC password memory row (type once / "remember for WEB DESKTOP" from the gated KEYS row / forget) + `localStorage['ghrdp.vncPass']` + `w.postMessage(..., exact origin)` with the webdesk launch deliberately NOT `noopener` (F9o/F10 'noopener' assertion superseded; the shim drops its opener right after the handoff). §3 RDP USAGE timer replaces logon age: ghrdp-server.ps1 writes `rdp-usage.ps1` (5s tick) adding +5 ONLY while `qwinsta` shows an Active rdp-tcp session OR a webdesk client is attached (Rust-dash ws-clients.txt, an Established 7333 socket, or a websockify-log connect newer than its disconnect); freeze <=20s when both drop, resume on reconnect, persist into config.json (rdpUsageSec/rdpUsageAt/rdpUsageHost) on every stop + every 30s, carried forward by main.yml only when rdpUsageHost matches the same host; UI headline = RDP USAGE (badge live/frozen, logon age kept as an honest fallback). §4 unchanged-but-gated (15s `tailscale ping -c 1 -timeout 3s` -> pingMs/direct|relay, WS RTT from the Rust dash, fps, UDP-41641 relay advisory, noVNC compression=6, TightVNC Poll*/CompareFB). §5 provisioning: qBittorrent as a PLAIN cached `winget download/install qBittorrent.qBittorrent` app (cache dir on the existing actions/cache path; no Web UI wiring, no autostart); ALL host-helper launches are now `-WindowStyle Hidden` (tailscale MSI, websockify deploy/self-test/keepalive-heal) or SYSTEM scheduled tasks, and the F9n/F9l 'websockify must NOT be hidden' assertions are superseded by the F11-5.3 contract (tests/workflow-webdesk.test.js updated). §6 setup time: 'Record job start time' merged into the cache-dir step, the post-MSI sleep replaced by a bounded service-registration wait, plus a 'Setup time report (F11-6)' step emitting `::notice title=Setup time::provisioning finished in <min> minutes` (target <=6 warm). Gates: launch-gates F11-2/F11-3/F11-4/F11-5.6 (+ the F10 piracy gate narrowed: bare 'torrent' is allowed only for the plain qBittorrent app; index strings/URLs and torrent bookmarks stay banned) + tests/f11-ux-package.test.js (REAL shim executed under vm with a DOM stub). §7 lab: autologin-lab.yml cells H (real shim auto-fill; wrong origin/non-opener/wrong type ignored; zero log output), I (REAL server + REAL usage loop: tick -> freeze <=20s -> resume -> config persistence -> continuity across a server restart), J (plain cached qBittorrent present) added; a/b/c peripheral and visual-confirmation cells stay PENDING-USER. Physical-PC rows (first-session console windows, silent fullscreen, live redirects) remain PENDING-USER - no LIVE claim before the verify table.

## Queue (production maintenance)
- G1. Secret rotation (USER-driven, out of band): checklist = MIGRATION.md sec 1.8.
- G2. Git history rewrite (USER-gated 'history-rewrite-go'): plan = MIGRATION.md sec 4. Runs AFTER G1.
- G3. VPS provision: user runs payloads\Provision-GhrdpVps.ps1 (MIGRATION sec 1.7). Client cmdkey once per sec 1.4.
- G4. Actions decommission after G3 verified live AND G1 done: checklist = MIGRATION.md sec 2 (13 items).

## Residual flags
- Legacy script-installer/helper assets still staged by main.yml but no UI links and serving endpoints are 404; retire them during gated G4 cleanup, not as a new client launch path.
- ghrdp-uninstall.ps1 cmdkey /list+/delete kept for prior-stash cleanup (removes, never stashes).
- Rust main.rs audited by inspection; no local cargo toolchain (deferred to CI).
- F15 lab switches are lab-only and must never become a production default: `GHRDP_LAB_NOMSG=1` suppresses the MessageBox on headless runners (the dialog text is JSONL-logged BEFORE it would show, so the fail-visible path is still proven), `GHRDP_LAB_CMDKEY_TIMEOUT_MS` only SHORTENS the fixed 180000 cmdkey bound, `GHRDP_LAB_KEEP_RDP=1` skips the temp .rdp delete. Production always shows dialogs and uses the full 180s bound.
- F9n web desktop transport (this session; supersedes the F9k/F9l/F9m serve cascade, which is dead on Windows runners): `tailscale serve` retired repo-wide for webdesk after the 2026-09-25 Tailscale 1.102.4 root cause (every post-connect LocalAPI call 401s: node owned by NT AUTHORITY\SYSTEM). Webdesk deploy now reads config .rdpIp (CGNAT, fail-closed tailnet-ip-unavailable), binds websockify <rdpIp>:7333, creates the idempotent CGNAT-only GHRDP-Webdesk firewall rule, and advertises http://<rdpIp>:7333/vnc.html directly; self-test curls the exact URL 3x5s, heals via the launcher, and classifies backend-dead|firewall|marker-missing (diagnostic-only; locked serve-failed/self-test-failed kept). Zero tailscale CLI calls in webdesk steps. Server+UI accept tailnet-http or legacy https ts.net (allowlist). Lab (webdesk-lab.yml) must go green BEFORE the PR; PROOF C (real VNC login + visible desktop) still requires a live dispatch + user click.
- F25 multi-source tailnet IP (this session): the webdesk step's tailnet IP is now an ordered, CGNAT-validated LADDER - L1 config .rdpIp | L2 config .runnerResolvedIP, else Resolve-DnsName(.dnsName) first A record | L3 step env RUNNER_RESOLVED_IP - first valid source wins. CULPRIT: 95f36ddd (F9n-1, merged via PR #28 / 07784606) replaced the serve URL with a SINGLE-source config .rdpIp read plus a fail-closed 'tailnet-ip-unavailable' throw; the stage step's `tailscale ip -4` is the same post-connect LocalAPI call that F9n itself proved 401s (node owned by NT AUTHORITY\SYSTEM), so .rdpIp was written empty while the F18-validated CGNAT address sat one property away - and the throw killed the whole job (no live runner, no dashboard, no RDP, no webdesk). `tailscale ip` is STILL never called in webdesk. The ladder pattern is anchored end-to-end ($cgnat = '^100\.(6[4-9]|[7-9]\d|1[01]\d|12[0-7])\.(\d{1,3})\.(\d{1,3})$'); the bare §2 prefix accepted '100.83.13.45evil', which would have been pasted into the advertised URL (found by the Node suite, not by inspection). FIX B: the stage writer now falls back to RUNNER_RESOLVED_IP and then READS config.json BACK, throwing 'config-writer lost tailnet IP' at the point of loss instead of two steps downstream. Writer audit (tests/config-writer-audit.py, 47 step regions): every existing config.json writer was already read-modify-write of the full object - nothing needed converting - and the gate now pins that invariant. Verify line: '[webdesk] tailnet IP source = L<n>'. Lab cell (webdesk-lab.yml) extracts the ladder verbatim from main.yml and runs scenarios (a) L3 recovery, (b) L1 skipped -> L2, (c) loud throw with the per-source dump; session token lacks actions:write, so it needs a user 'Run workflow' click.
- F9l(502) web desktop: the self-test now heals + classifies, and every halt leaves a webdesk-diag artifact (so the next failure is diagnosable without the VM). PROOF C (real VNC login + visible desktop) still requires a live dispatch.
- PR #22 merged by Arena at 2026-09-25T05:28:48Z as merge commit a81a926; it includes F9l-1..5 but not the later no-hidden-websockify follow-up 3830bc0. That follow-up is pushed to the session branch and needs its own PR/merge before dispatching main.yml. CI + the fallback lab pass on 3830bc0; no post-F9l production run has completed. After the follow-up merge, dispatch main.yml, confirm web-desktop + self-test green, then Proof C still requires a real VNC login and visible desktop (do not claim WEB DESKTOP LIVE before that). The last live run before F9l, 2026-09-25T04:03Z, failed at serve on Tailscale 1.102.4; if serve fails again, main.yml now logs every redacted attempt output, CLI version/help, serve tail, and raw status for triage.

## Anchors (re-derive by search)
- main.yml: TS up fail-closed :213 (redactor :223, Emit-SecretHalt :237, missing-guard :288, classify+halt :330); MagicDNS gates ~:344/:396/:876; F9g retry ~:899. Web-desktop step ~:1086 (F9h guard ~:1120, belt ~:1148, F9n rdpIp ~:1170, firewall ~:1325, URL ~:1338); self-test ~:1386. ghrdp-server.ps1: native-status ~:381 (F9n allowlist ~:506); 404 guard ~:238. payloads/main.rs: api_config secret-strip ~:283. helper: mutex ~:105. MIGRATION.md: web desktop sec 1.10 ~:294 (F9n serve-retired + history). - [F28] ghrdp-server.ps1: scanner block :248-410 (interval 30 / stale 90 / window 3600); startup scan :1874; 30s tick in the accept loop :1928; native-status live CredSSP probe :939 + $rlOut merge :986 (authLast/logonCollector/credsspLive); beacon allowlist :1065. ui.html: credsspVerdict :1568, logonRowText :1783 (row + RED collector-dead), recoveryDecision :1811, paintRecovery :1846, ghrdpRecredUrl :1878, recovery click :1958; recoveryRow HTML :401. ghrdp-rdp-launcher.cs: --fallback-selftest :554, CredentialFallbackDecision :835, CmdkeyStep outcome :848, MstscStep nativePrompt/beacon :954, RecredStep :1165. launch-gates.yml: F28 bash gate :1464; F28 windows steps :1662/:1712. autologin-lab.yml: cell V :1624; matrix row (v) :1784.
